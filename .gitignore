.env
.env.local
*.env.local

# Per-developer Claude Code permissions (contains absolute paths / user-specific allow lists)
.claude/settings.local.json
**/.claude/settings.local.json

# Runtime storage
Backend/storage/
storage/

# Tesseract trained data (binary, large — install separately)
*.traineddata

# Backend API project storage (PII receipt PDFs)
Backend/src/TaxReader.Api/storage/

# MSBuild diagnostic logs / binlogs
build-diag*.txt
*.binlog

# Local secrets — loose API key files used for local E2E runs (never commit)
keyfile.txt
*.key
