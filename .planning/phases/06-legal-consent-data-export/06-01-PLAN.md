---
phase: 06-legal-consent-data-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend/src/app/(legal)/impressum/page.tsx
  - Frontend/src/app/(legal)/datenschutz/page.tsx
  - Frontend/src/app/(legal)/agb/page.tsx
  - Frontend/src/app/(legal)/widerruf/page.tsx
  - Frontend/src/app/(legal)/layout.tsx
  - Frontend/src/components/layout/footer.tsx
  - Frontend/src/components/layout/cookie-settings-link.tsx
  - Frontend/src/app/(authenticated)/layout.tsx
  - Frontend/src/providers/auth-provider.tsx
  - .planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md
autonomous: false
requirements: [LEG-01, LEG-02, LEG-03, LEG-04]
must_haves:
  truths:
    - "Impressum, Datenschutzerklärung, AGB, Widerrufsbelehrung pages exist and render"
    - "Every page footer links Impressum, Datenschutz, AGB, Widerruf, and Cookie-Einstellungen"
    - "Datenschutz lists Anthropic, Stripe, Sentry, BetterStack as sub-processors with AVV links + Drittland note"
    - "AGB states StBerG-safe positioning and GoBD non-applicability"
    - "Every legal page renders the '⚠ Entwurf – anwaltliche Prüfung ausstehend' marker"
    - "Unauthenticated users can reach /agb and /widerruf without redirect"
  artifacts:
    - path: "Frontend/src/app/(legal)/agb/page.tsx"
      provides: "AGB page with StBerG + GoBD + Widerrufsrecht + VSBG sections"
      contains: "Vertragsgegenstand ist Strukturierung, keine Steuerberatung"
    - path: "Frontend/src/app/(legal)/widerruf/page.tsx"
      provides: "Widerrufsbelehrung + Muster-Widerrufsformular"
      contains: "Muster-Widerrufsformular"
    - path: "Frontend/src/components/layout/footer.tsx"
      provides: "Site-wide footer with five legal/consent links"
      min_lines: 15
    - path: ".planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md"
      provides: "Lawyer-review gate checklist (Drafted → Lawyer-reviewed → Live)"
      contains: "Lawyer-reviewed"
  key_links:
    - from: "Frontend/src/app/(legal)/layout.tsx"
      to: "Frontend/src/components/layout/footer.tsx"
      via: "import + mount after <main>"
      pattern: "import.*footer"
    - from: "Frontend/src/components/layout/footer.tsx"
      to: "/agb /widerruf /impressum /datenschutz"
      via: "next/link hrefs"
      pattern: "href=\"/agb\""
    - from: "Frontend/src/providers/auth-provider.tsx"
      to: "PUBLIC_PATHS"
      via: "array includes /agb and /widerruf"
      pattern: "/agb"
---

<objective>
Author full German DRAFT copy for the four mandated legal pages (Impressum, Datenschutzerklärung, AGB, Widerrufsbelehrung), create a site-wide Footer that links all four plus "Cookie-Einstellungen" from every page, fix the BelegPilot→TaxReader branding in the legal layout, add /agb and /widerruf to PUBLIC_PATHS, and create the lawyer-review gate doc.

Purpose: Satisfy LEG-01..LEG-04 — the launch-blocking legal posture for a commercial DE SaaS. Every page must carry an unmissable draft marker until lawyer sign-off (Phase 7 / QA-07).
Output: Four rendered legal pages, a Footer component mounted in (legal) and (authenticated) layouts, updated PUBLIC_PATHS, and 06-LEGAL-REVIEW.md.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-legal-consent-data-export/06-CONTEXT.md
@.planning/phases/06-legal-consent-data-export/06-RESEARCH.md
@.planning/phases/06-legal-consent-data-export/06-PATTERNS.md
@.planning/phases/06-legal-consent-data-export/06-UI-SPEC.md

<interfaces>
<!-- Exact existing shapes the executor needs — do not re-explore. -->

Frontend/src/app/(legal)/layout.tsx (current — lines 1-24, MUST modify):
- Renders `<span className="text-lg font-bold tracking-tight">BelegPilot</span>` and a `"B"` avatar div. RENAME both to "TaxReader" / "T".
- Ends with `<main>{children}</main>` — add `<Footer />` after it.
- Header: `<header className="flex h-14 shrink-0 items-center justify-between border-b bg-card px-6">`

Frontend/src/providers/auth-provider.tsx (current — line ~29, MUST modify):
- `const PUBLIC_PATHS = ["/login", "/register", "/impressum", "/datenschutz"];`
- Change to: `["/login", "/register", "/impressum", "/datenschutz", "/agb", "/widerruf"]`

Frontend/src/app/(legal)/impressum/page.tsx (analog for all legal pages):
- Server Component, default export, `export const metadata = { title: "..." }`
- Container: `<div className="mx-auto max-w-2xl space-y-8 px-6 py-10 text-sm leading-relaxed">`
- `<h1 className="text-2xl font-semibold tracking-tight">`
- Sections: `<h2 className="mb-2 text-base font-semibold">` + `<p className="text-muted-foreground">`
- Links: `<Link href="..." className="text-primary hover:underline">`

Frontend/src/app/(authenticated)/layout.tsx (current — full file, MUST modify):
- `"use client"`; `<SidebarProvider><AppSidebar /><SidebarInset className="overflow-hidden">{children}</SidebarInset></SidebarProvider>`
- Add `<Footer />` as a sibling of `<SidebarInset>` (inside SidebarProvider) to avoid the overflow-hidden clip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer + CookieSettingsLink + fix (legal)/layout branding + PUBLIC_PATHS + mount footer</name>
  <read_first>
    - Frontend/src/components/layout/header.tsx (footer structural analog — per 06-PATTERNS.md)
    - Frontend/src/app/(legal)/layout.tsx (modify — branding + footer mount)
    - Frontend/src/app/(authenticated)/layout.tsx (modify — footer mount)
    - Frontend/src/providers/auth-provider.tsx (modify — PUBLIC_PATHS)
    - 06-UI-SPEC.md § "Footer mounting" + "Component Inventory" (footer markup)
    - 06-RESEARCH.md Pitfall 6 (PUBLIC_PATHS) + Pitfall 8 (branding)
  </read_first>
  <action>
    Create `Frontend/src/components/layout/footer.tsx` as a Server Component (NO "use client"). Markup per 06-UI-SPEC.md § "Footer mounting":
    ```tsx
    import Link from "next/link";
    import { CookieSettingsLink } from "@/components/layout/cookie-settings-link";

    export function Footer() {
      return (
        <footer className="border-t bg-card px-6 py-4">
          <div className="flex flex-wrap items-center gap-x-4 gap-y-2 text-xs text-muted-foreground">
            <Link href="/impressum" className="hover:text-foreground hover:underline">Impressum</Link>
            <Link href="/datenschutz" className="hover:text-foreground hover:underline">Datenschutzerklärung</Link>
            <Link href="/agb" className="hover:text-foreground hover:underline">AGB</Link>
            <Link href="/widerruf" className="hover:text-foreground hover:underline">Widerrufsbelehrung</Link>
            <CookieSettingsLink />
          </div>
        </footer>
      );
    }
    ```
    Create `Frontend/src/components/layout/cookie-settings-link.tsx` as a Client Component (`"use client"`). It renders a `<button>` with text "Cookie-Einstellungen" styled `hover:text-foreground hover:underline text-xs text-muted-foreground`. Plan 06-02 wires the actual `useConsent().reopenSettings()` call; for THIS plan, make it call `useConsent().reopenSettings()` ONLY if the hook exists — since ConsentProvider lands in 06-02, guard it: import `useConsent` is NOT yet available, so for this plan render the button with an `onClick` that is a no-op placeholder `() => {}` and a `// TODO(06-02): wire reopenSettings` comment. (06-02 will replace the no-op.) The button must exist now so the footer link is present for LEG-01 footer-reachability.
    In `(legal)/layout.tsx`: change `<span>BelegPilot</span>` → `<span>TaxReader</span>` and the `"B"` avatar text → `"T"`. Import `{ Footer }` from `@/components/layout/footer` and mount `<Footer />` immediately after `</main>`.
    In `(authenticated)/layout.tsx`: import `{ Footer }` and mount `<Footer />` as a sibling of `<SidebarInset>` inside `<SidebarProvider>` (NOT inside SidebarInset — avoids overflow-hidden clip per RESEARCH Open Question 3).
    In `auth-provider.tsx`: change `PUBLIC_PATHS` to `["/login", "/register", "/impressum", "/datenschutz", "/agb", "/widerruf"]`.
  </action>
  <acceptance_criteria>
    - `Frontend/src/components/layout/footer.tsx` exists and contains `href="/agb"`, `href="/widerruf"`, `href="/impressum"`, `href="/datenschutz"`
    - footer.tsx does NOT start with `"use client"` (it is a Server Component)
    - `Frontend/src/components/layout/cookie-settings-link.tsx` exists, starts with `"use client"`, and contains the text `Cookie-Einstellungen`
    - `(legal)/layout.tsx` contains `TaxReader` and does NOT contain `BelegPilot`
    - `(legal)/layout.tsx` contains `import { Footer }` and `<Footer />`
    - `(authenticated)/layout.tsx` contains `<Footer />`
    - `auth-provider.tsx` PUBLIC_PATHS line contains both `/agb` and `/widerruf`
  </acceptance_criteria>
  <verify>
    <automated>cd Frontend && npm run build</automated>
  </verify>
  <done>Footer renders five links on every (legal) and (authenticated) page; branding fixed; /agb and /widerruf are public; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Author Impressum + Datenschutz drafts (replace placeholders) + AGB + Widerruf new pages, all with DraftWarning</name>
  <read_first>
    - Frontend/src/app/(legal)/impressum/page.tsx (analog + replace target)
    - Frontend/src/app/(legal)/datenschutz/page.tsx (replace target — add sub-processor table)
    - 06-CONTEXT.md D-01 (content facts: §19 UStG no USt-IdNr.; sub-processors; AGB structure; Widerruf §356 text)
    - 06-CONTEXT.md <specifics> (Widerruf §356 statutory text locked from Phase 5; Kleinunternehmer note)
    - 06-RESEARCH.md § "Operator-Tracked Artifacts" (06-AVV-TRACKING.md sub-processor rows with DPA URLs)
    - 06-UI-SPEC.md § "Copywriting Contract" (page titles) + "Draft Warning component" (exact amber markup) + "Component Inventory" (Datenschutz table style)
    - 06-PATTERNS.md § agb/page.tsx + widerruf/page.tsx (structure)
  </read_first>
  <action>
    Add a shared `DraftWarning` function to each of the four legal pages (inline per file — Server Components, no shared client import needed), placed at the TOP of the content `<div>` BEFORE `<h1>`, with EXACT markup from 06-UI-SPEC.md § "Draft Warning component":
    ```tsx
    function DraftWarning() {
      return (
        <div className="rounded border border-amber-400 bg-amber-50 dark:bg-amber-500/10 px-4 py-2 text-sm text-amber-800 dark:text-amber-200">
          ⚠ Entwurf – anwaltliche Prüfung ausstehend
        </div>
      );
    }
    ```
    All four pages use the container `<div className="mx-auto max-w-2xl space-y-8 px-6 py-10 text-sm leading-relaxed">`, `metadata` title per the Copywriting Contract ("Impressum", "Datenschutzerklärung", "Allgemeine Geschäftsbedingungen", "Widerrufsbelehrung" — append " – TaxReader"), `<h1 className="text-2xl font-semibold tracking-tight">`, sections with `<h2 className="mb-2 text-base font-semibold">`, body `<p className="text-muted-foreground">`. All copy German Sie-form.

    REPLACE `(legal)/impressum/page.tsx` content (LEG-01, TMG §5): h1 "Impressum"; sections — "Angaben gemäß § 5 TMG" with placeholders `[Name]`, `[Anschrift]`, `[PLZ Ort]`; "Kontakt" with `E-Mail: [kontakt@…]`; a Kleinunternehmer note "Gemäß § 19 UStG wird keine Umsatzsteuer berechnet; es wird keine USt-IdNr. geführt."; "Verbraucherstreitbeilegung / Online-Streitbeilegung" with the ODR link `https://ec.europa.eu/consumers/odr/`. NO USt-IdNr. field (D-01).

    REPLACE `(legal)/datenschutz/page.tsx` content (LEG-02): h1 "Datenschutzerklärung"; sections covering DSGVO Art. 13 (Zwecke, Rechtsgrundlagen Art. 6, Datenkategorien: Belege/Positionen/Klassifizierungen/Konto/Zahlungen), Art. 22 (automatisierte Klassifizierung mit menschlicher Überprüfung/Override — descriptive, NOT advice), Art. 28 sub-processor table. Render a `<table className="text-sm border-collapse">` with header row (Dienst / Zweck / Sitz / AVV) and one `<tr className="border-b border-border">` per sub-processor:
      - Anthropic — KI-Klassifizierung — USA — `<Link href="https://www.anthropic.com/legal/dpa">DPA</Link>`
      - Stripe — Zahlungsabwicklung — Irland/USA — `https://stripe.com/de/legal/dpa`
      - Sentry — Fehleranalyse — EU (Functional Software, USA) — `https://sentry.io/legal/dpa/`
      - BetterStack — Uptime-Monitoring — EU — `https://betterstack.com/privacy`
    Add a Drittland-Übermittlung section naming Anthropic (USA), referencing Schrems II / TADPF (EU-U.S. Data Privacy Framework) with a link to the Anthropic certification/DPA. Add Art. 15/Art. 20 self-service note: "Sie können Ihre Daten jederzeit über die Einstellungen exportieren."

    CREATE `(legal)/agb/page.tsx` (LEG-03): h1 "Allgemeine Geschäftsbedingungen"; sections — "§ 1 Vertragsgegenstand" containing the EXACT phrase "Vertragsgegenstand ist Strukturierung, keine Steuerberatung" and a StBerG-safe statement ("TaxReader ist ein Hilfsmittel zur Strukturierung von Belegen und leistet keine Steuerberatung im Sinne des StBerG."); "§ 2 GoBD" stating non-applicability ("TaxReader ist kein GoBD-zertifiziertes Aufbewahrungssystem; die Aufbewahrungspflicht verbleibt beim Nutzer."); "§ 3 Widerrufsrecht" referencing `<Link href="/widerruf">Widerrufsbelehrung</Link>`; "§ 4 Erstattung" (refund policy, token-pack basis); "§ 5 Support" with the conservative SLA "Wir bemühen uns, Anfragen innerhalb von 5 Werktagen zu beantworten." (flag for lawyer in 06-LEGAL-REVIEW.md per RESEARCH Open Question 2); "§ 6 Streitbeilegung" with VSBG signpost + ODR link.

    CREATE `(legal)/widerruf/page.tsx` (LEG-04): h1 "Widerrufsbelehrung"; full Widerrufsbelehrung text (14-Tage-Frist, Widerrufsfolgen); a "Muster-Widerrufsformular" section (the standard BGB Anlage 2 template with placeholders `[An: Name/Anschrift]`, "Hiermit widerrufe(n) ich/wir …"); and the §356 Abs. 4 BGB waiver note locked from Phase 5: "Ich verlange ausdrücklich, dass mit der Ausführung des Vertrags sofort begonnen wird. Mir ist bekannt, dass ich hierdurch mein Widerrufsrecht verliere." Each page imports `Link` from `next/link` where links are used.
  </action>
  <acceptance_criteria>
    - `(legal)/impressum/page.tsx` contains `§ 5 TMG` and `§ 19 UStG` and `ec.europa.eu/consumers/odr` and does NOT contain `USt-IdNr.: DE` (no real USt-ID)
    - `(legal)/datenschutz/page.tsx` contains all four: `Anthropic`, `Stripe`, `Sentry`, `BetterStack`
    - `(legal)/datenschutz/page.tsx` contains `anthropic.com/legal/dpa` and `Art. 22` and `Schrems` (or `TADPF`/`Data Privacy Framework`)
    - `(legal)/agb/page.tsx` contains the exact string `Vertragsgegenstand ist Strukturierung, keine Steuerberatung`
    - `(legal)/agb/page.tsx` contains `GoBD` and `StBerG` and `VSBG` and `<Link href="/widerruf"`
    - `(legal)/widerruf/page.tsx` contains `Muster-Widerrufsformular` and `mein Widerrufsrecht verliere`
    - All four page files contain the string `⚠ Entwurf – anwaltliche Prüfung ausstehend`
    - `grep -rL "Entwurf" Frontend/src/app/(legal)/impressum/page.tsx Frontend/src/app/(legal)/datenschutz/page.tsx Frontend/src/app/(legal)/agb/page.tsx Frontend/src/app/(legal)/widerruf/page.tsx` returns no files (all four contain the marker)
  </acceptance_criteria>
  <verify>
    <automated>cd Frontend && npm run build</automated>
  </verify>
  <done>Four legal pages render with full German draft copy, draft markers, sub-processor table, and StBerG/GoBD/Widerruf content; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Create 06-LEGAL-REVIEW.md gate doc</name>
  <read_first>
    - 06-CONTEXT.md D-02 (lawyer-review gate)
    - 06-RESEARCH.md § "06-LEGAL-REVIEW.md columns" (table structure + status values)
  </read_first>
  <action>
    Create `.planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md` with: a heading, a one-paragraph explanation that the draft markers are removed per-page ONLY when that page's row reaches "Lawyer-reviewed", final sign-off in Phase 7 (QA-07). Include a markdown table with columns `Page | Status | Lawyer | Notes` and one row each for Impressum, Datenschutzerklärung, AGB, Widerrufsbelehrung — all Status = `Drafted`. Document the status flow `Drafted → Lawyer-reviewed → Live`. Add a Notes entry on the AGB row flagging the 5-Werktage support-SLA assumption for lawyer confirmation (per RESEARCH Open Question 2).
  </action>
  <acceptance_criteria>
    - `.planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md` exists
    - file contains `Lawyer-reviewed` and `Drafted` and `Live`
    - file contains all four page names: `Impressum`, `Datenschutzerklärung`, `AGB`, `Widerrufsbelehrung`
    - file contains `5 Werktage` or `5 Werktagen` (the flagged SLA assumption)
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md" && grep -q "Lawyer-reviewed" ".planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md" && echo OK</automated>
  </verify>
  <done>Review-gate doc exists tracking all four pages from Drafted to Live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Four German draft legal pages (Impressum, Datenschutzerklärung, AGB, Widerrufsbelehrung) with unmissable amber draft markers, a site-wide footer linking all four plus Cookie-Einstellungen, fixed TaxReader branding, public access to /agb and /widerruf, and the 06-LEGAL-REVIEW.md gate.</what-built>
  <how-to-verify>
    1. Run `cd Frontend && npm run dev`.
    2. As an UNAUTHENTICATED user, visit http://localhost:3000/impressum, /datenschutz, /agb, /widerruf — all four must load (no redirect to /login) and show the amber "⚠ Entwurf – anwaltliche Prüfung ausstehend" marker at the top.
    3. Confirm the Datenschutz page shows a sub-processor table with Anthropic, Stripe, Sentry, BetterStack and clickable AVV links.
    4. Confirm the AGB page states "Vertragsgegenstand ist Strukturierung, keine Steuerberatung" and references GoBD.
    5. Confirm the footer appears on every page (legal + authenticated app pages) with all five links, and the header logo reads "TaxReader" (not BelegPilot).
    6. NOTE: lawyer review itself is an external operator task tracked in 06-LEGAL-REVIEW.md and finalized in Phase 7 — do not remove draft markers yet.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. missing field, wrong copy, broken link).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → (legal) pages | Public legal pages must be reachable without auth; no user data rendered |
| public footer link → ConsentSettingsLink | Client component triggers consent UI; no privileged action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Legal pages exposing placeholder operator PII | accept | Pages contain only operator-supplied public Impressum data (legally mandated to be public under TMG §5); placeholders `[Name]`/`[Anschrift]` filled by operator before launch |
| T-06-02 | Tampering | Shipping unreviewed legal text as "final" | mitigate | Mandatory amber draft marker on all four pages (grep-verified in acceptance_criteria); removal gated by 06-LEGAL-REVIEW.md "Lawyer-reviewed" status; blocking HUMAN-UAT |
| T-06-03 | Elevation of Privilege | /agb /widerruf bypassing auth-gate unintentionally exposing app | accept | Only static legal Server Components are made public via PUBLIC_PATHS; no authenticated data routes added to the list |
</threat_model>

<verification>
- `cd Frontend && npm run build` exits 0
- All four legal pages contain the draft marker (grep)
- Footer links present on both layouts; branding shows TaxReader
- PUBLIC_PATHS includes /agb and /widerruf
- 06-LEGAL-REVIEW.md exists with all four page rows
</verification>

<success_criteria>
LEG-01..LEG-04 drafted: four legal pages render with full German draft copy and draft markers, footer links all four plus Cookie-Einstellungen from every page, Datenschutz lists all four sub-processors with AVV links + Drittland note, AGB carries StBerG/GoBD positioning, /agb and /widerruf are public, lawyer-review gate doc created. Human-UAT approved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-legal-consent-data-export/06-01-SUMMARY.md`
</output>
