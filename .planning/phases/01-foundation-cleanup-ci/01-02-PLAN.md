---
phase: 01-foundation-cleanup-ci
plan: 02
type: execute
wave: 4
depends_on: [01, 03, 04]
files_modified:
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements:
  - FND-04
  - FND-05
must_haves:
  truths:
    - "`.github/workflows/ci.yml` exists and triggers on `pull_request` to `main` and `push` to `main`"
    - "Three jobs (`hygiene-check`, `backend-build-test`, `frontend-lint-build`) run in parallel and exit non-zero on failure"
    - "`hygiene-check` fails the build if any of `storage/`, `Backend/storage/`, `Backend/src/TaxReader.Api/storage/`, `build-diag*.txt`, or `*.binlog` appears in the working tree"
    - "`backend-build-test` runs `dotnet restore` + `dotnet build --configuration Release` + `dotnet test --configuration Release` against the `Backend/` directory using `actions/setup-dotnet@v4` with `dotnet-version: '10.0.x'`"
    - "`frontend-lint-build` runs `npm ci` + `npm run lint` + `npm run build` in `Frontend/` using `actions/setup-node@v4` with `node-version: '22'`"
    - "Concurrency group cancels in-progress runs for the same PR ref but never cancels `main`"
    - "Top-level `README.md` exists and documents prerequisites (.NET 10 SDK, Node 22+, Docker Desktop, Tesseract for non-container dev), env-var setup (`cp .env.example .env`), and the canonical run command (`docker compose up --build`)"
    - "`README.md` documents the post-startup browser URL (`https://localhost`) and links to `CLAUDE.md` plus `.planning/codebase/`"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking CI gate covering hygiene, backend, and frontend"
      contains: "hygiene-check"
    - path: "README.md"
      provides: "New-developer onboarding from clone to running stack"
      contains: "docker compose up --build"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Backend/Directory.Packages.props"
      via: "actions/setup-dotnet@v4 cache-dependency-path includes the central NuGet manifest"
      pattern: "Backend/Directory.Packages.props"
    - from: ".github/workflows/ci.yml"
      to: "Frontend/package-lock.json"
      via: "actions/setup-node@v4 cache-dependency-path"
      pattern: "Frontend/package-lock.json"
    - from: "README.md"
      to: ".env.example"
      via: "Quick-start docs reference `cp .env.example .env`"
      pattern: ".env.example"
---

<objective>
Ship the merge-blocking CI workflow (`.github/workflows/ci.yml`) and the top-level `README.md`. CI runs three parallel jobs — `hygiene-check`, `backend-build-test`, `frontend-lint-build` — on every PR targeting `main` and every push to `main`. README documents prerequisites + quick-start so a new developer can clone, configure `.env`, and reach the running app.

Purpose: Phase 1 Success Criterion #1 (every PR has the four merge-blocking checks), #2 (CI fails if `storage/` / `build-diag.txt` is reintroduced), and #6 (new dev can run `docker compose up --build` from `README.md` instructions).
Output: 1 new workflow file, 1 new top-level README, and a manual-only follow-up to enable branch protection on `main`.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Programming/Repos/TaxReader/CLAUDE.md
@D:/Programming/Repos/TaxReader/.planning/ROADMAP.md
@D:/Programming/Repos/TaxReader/.planning/REQUIREMENTS.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-CONTEXT.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-RESEARCH.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-PATTERNS.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-VALIDATION.md
@D:/Programming/Repos/TaxReader/.planning/codebase/CONVENTIONS.md
@D:/Programming/Repos/TaxReader/.planning/codebase/ARCHITECTURE.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-01-SUMMARY.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-03-SUMMARY.md
@D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-04-SUMMARY.md

<interfaces>
<!-- Key shapes the executor needs. Extracted from RESEARCH.md verbatim-copyable patterns. -->

This plan creates two NEW files. There are no existing analogs in the repo (no prior `.github/workflows/`, no top-level `README.md`). The closest precedents:
- `Backend/README.md` and `Frontend/README.md` exist but are scoped to their subprojects — top-level README is a new role per PATTERNS.md.
- No CI workflow exists today — RESEARCH.md Pattern 8 (hygiene-check) and Pattern 9 (CI skeleton) are the verbatim-copyable templates.

**Wave assignment rationale:** This plan is Wave 4 (depends on plans 01, 03, 04) because the CI's `backend-build-test` job will run all unit tests, including the Wave 0 tests added by:
- Plan 01-01: `AnthropicOptionsTests.cs`, `CorsConfigurationTests.cs`
- Plan 01-03: `SentryScrubbingTests.cs`
- Plan 01-04: `SerilogEnrichmentTests.cs`

Running CI before those tests are committed would yield false-green coverage. Strict ordering ensures the first CI green tick reflects the full Phase 1 surface.

From RESEARCH.md Pattern 9 — verbatim CI skeleton:

```yaml
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  hygiene-check:
    # … as in Pattern 8 …

  backend-build-test:
    name: Backend build + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: |
            Backend/**/packages.lock.json
            Backend/Directory.Packages.props
      - name: Restore
        working-directory: Backend
        run: dotnet restore
      - name: Build
        working-directory: Backend
        run: dotnet build --no-restore --configuration Release
      - name: Test
        working-directory: Backend
        run: dotnet test --no-build --configuration Release --verbosity normal

  frontend-lint-build:
    name: Frontend lint + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: Frontend/package-lock.json
      - name: Install
        working-directory: Frontend
        run: npm ci
      - name: Lint
        working-directory: Frontend
        run: npm run lint
      - name: Build
        working-directory: Frontend
        run: npm run build
```

From RESEARCH.md Pattern 8 — verbatim hygiene-check job body:

```yaml
  hygiene-check:
    name: Hygiene check (no PII / build artifacts)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify no leaked storage / build artifacts
        shell: bash
        run: |
          set -e
          violations=()
          for path in \
            "storage" \
            "Backend/storage" \
            "Backend/src/TaxReader.Api/storage" ; do
            if [ -d "$path" ]; then
              violations+=("Forbidden directory: $path")
            fi
          done

          while IFS= read -r f; do
            violations+=("Forbidden file: $f")
          done < <(find . -type f \
            \( -name 'build-diag*.txt' -o -name '*.binlog' \) \
            -not -path './.git/*' 2>/dev/null)

          if [ ${#violations[@]} -gt 0 ]; then
            printf 'Hygiene check FAILED:\n'
            printf '  - %s\n' "${violations[@]}"
            exit 1
          fi
          printf 'Hygiene check passed.\n'
```

**Caveat (RESEARCH.md Pitfall 5):** Central Package Management does NOT auto-generate `packages.lock.json` files. The `cache-dependency-path` glob `Backend/**/packages.lock.json` matches zero files until `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>` is enabled. Including `Backend/Directory.Packages.props` in the same multi-line glob ensures cache invalidation still works on version bumps. Verify on first CI run. If lock files are needed, add `<RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>` to `Backend/Directory.Build.props` in a follow-up — out of scope for this plan.

</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .github/workflows/ci.yml with hygiene-check + backend-build-test + frontend-lint-build jobs</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <read_first>
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-CONTEXT.md (D-06 — `hygiene-check` CI job is mandatory belt-and-suspenders alongside `.gitignore`; D-08, D-09, D-10, D-11 — locked decisions: parallel jobs, no Postgres in CI, branch-protection requires the three job names, no CI secrets in Phase 1)
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-RESEARCH.md (Pattern 8 — verbatim hygiene-check job body, lines 658-695; Pattern 9 — verbatim CI skeleton, lines 697-771; Pitfall 5 — CPM cache invalidation caveat)
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-PATTERNS.md (`.github/workflows/ci.yml` row in the file classification table — establishes new pattern, no codebase analog)
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-01-SUMMARY.md (Plan 01-01's deliverables — confirm `.gitignore` and `Backend/.dockerignore` are in place so the hygiene-check baseline runs green)
    - D:/Programming/Repos/TaxReader/Backend/Directory.Packages.props (confirm structure — central package management; cache-dependency-path glob will reference this file)
    - D:/Programming/Repos/TaxReader/Frontend/package-lock.json (confirm exists at this path — `actions/setup-node@v4` cache-dependency-path)
    - D:/Programming/Repos/TaxReader/Frontend/package.json (confirm `lint` and `build` npm scripts both exist; current scripts are: `dev`, `build`, `start`, `lint`)
  </read_first>
  <behavior>
    - Test 1 (smoke / structural): file `.github/workflows/ci.yml` exists, parses as valid YAML, has top-level keys `name`, `on`, `concurrency`, `jobs` exactly
    - Test 2 (structural): the `jobs` map has exactly three keys: `hygiene-check`, `backend-build-test`, `frontend-lint-build` — no extras
    - Test 3 (structural): each job declares `runs-on: ubuntu-latest` and uses `actions/checkout@v4`
    - Test 4 (structural): `backend-build-test` uses `actions/setup-dotnet@v4` with `dotnet-version: '10.0.x'` and a `cache-dependency-path` containing `Backend/Directory.Packages.props`
    - Test 5 (structural): `frontend-lint-build` uses `actions/setup-node@v4` with `node-version: '22'` and `cache-dependency-path: Frontend/package-lock.json`
    - Test 6 (manual / smoke after merge): the first PR raised against `main` shows the three jobs running in parallel; all three pass green; `pull_request` and `push` triggers both fire on subsequent commits
    - Test 7 (manual / negative): a follow-up commit that re-introduces `Backend/src/TaxReader.Api/storage/` (or a `build-diag*.txt` file) causes the `hygiene-check` job to exit non-zero with the violation message
  </behavior>
  <action>
    Create the directory `.github/workflows/` (NEW — does not exist in the repo) and the file `.github/workflows/ci.yml` (NEW). The full file content combines RESEARCH.md Pattern 9 (skeleton) with Pattern 8 (hygiene-check job body). Inline the full file content verbatim — do NOT split across files:

    ```yaml
    name: CI

    on:
      pull_request:
        branches: [main]
      push:
        branches: [main]

    # Cancel in-progress runs for the same PR ref; never cancel main.
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}

    jobs:
      hygiene-check:
        name: Hygiene check (no PII / build artifacts)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - name: Verify no leaked storage / build artifacts
            shell: bash
            run: |
              set -e
              violations=()
              for path in \
                "storage" \
                "Backend/storage" \
                "Backend/src/TaxReader.Api/storage" ; do
                if [ -d "$path" ]; then
                  violations+=("Forbidden directory: $path")
                fi
              done

              # Pattern globs — match any build-diag*.txt or *.binlog at any depth.
              while IFS= read -r f; do
                violations+=("Forbidden file: $f")
              done < <(find . -type f \
                \( -name 'build-diag*.txt' -o -name '*.binlog' \) \
                -not -path './.git/*' 2>/dev/null)

              if [ ${#violations[@]} -gt 0 ]; then
                printf 'Hygiene check FAILED:\n'
                printf '  - %s\n' "${violations[@]}"
                exit 1
              fi
              printf 'Hygiene check passed.\n'

      backend-build-test:
        name: Backend build + test
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-dotnet@v4
            with:
              dotnet-version: '10.0.x'
              # CPM cache key: include Directory.Packages.props because that's the
              # central NuGet version manifest. Including it prevents stale cache
              # restoration when versions are bumped (RESEARCH.md Pitfall 5).
              cache: true
              cache-dependency-path: |
                Backend/**/packages.lock.json
                Backend/Directory.Packages.props
          - name: Restore
            working-directory: Backend
            run: dotnet restore
          - name: Build
            working-directory: Backend
            run: dotnet build --no-restore --configuration Release
          - name: Test
            working-directory: Backend
            run: dotnet test --no-build --configuration Release --verbosity normal

      frontend-lint-build:
        name: Frontend lint + build
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '22'
              cache: 'npm'
              cache-dependency-path: Frontend/package-lock.json
          - name: Install
            working-directory: Frontend
            run: npm ci
          - name: Lint
            working-directory: Frontend
            run: npm run lint
          - name: Build
            working-directory: Frontend
            run: npm run build
    ```

    Notes for the executor:
    - File must be saved with LF line endings (Unix). The repo's `.gitattributes` (if present) governs; otherwise let git's default normalisation handle it.
    - Indentation is 2 spaces (YAML standard). No tabs.
    - The `concurrency.cancel-in-progress` value MUST be the literal string `${{ github.event_name == 'pull_request' }}` — that evaluates to `true` for PRs and `false` for direct pushes to `main`, satisfying D-08's "concurrency group … cancel-in-progress: true for PRs (not for `main`)".
    - Do NOT add any `permissions:` block — Phase 1 needs only the default `contents: read` permission, which is GitHub's default for public + private repos using GITHUB_TOKEN.
    - Do NOT add CI secrets — D-11 explicitly states no secrets are needed in Phase 1 (unit tests use in-memory EF; Sentry DSN is public; Anthropic key not used by tests).
    - Do NOT add a Postgres service container — D-09 defers integration tests to Phase 7's `QA-01`.

    After writing the file, validate locally:

    ```bash
    # Quick local sanity-check — confirm valid YAML
    python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML OK"

    # Confirm the three required job names appear at the expected indentation
    grep -E "^  (hygiene-check|backend-build-test|frontend-lint-build):" .github/workflows/ci.yml | wc -l   # expect 3
    ```
  </action>
  <verify>
    <automated>cd D:/Programming/Repos/TaxReader && test -f .github/workflows/ci.yml && grep -q "^  hygiene-check:" .github/workflows/ci.yml && grep -q "^  backend-build-test:" .github/workflows/ci.yml && grep -q "^  frontend-lint-build:" .github/workflows/ci.yml && grep -q "branches: \[main\]" .github/workflows/ci.yml && grep -qF "actions/setup-dotnet@v4" .github/workflows/ci.yml && grep -qF "dotnet-version: '10.0.x'" .github/workflows/ci.yml && grep -qF "actions/setup-node@v4" .github/workflows/ci.yml && grep -qF "node-version: '22'" .github/workflows/ci.yml && grep -qF "Backend/Directory.Packages.props" .github/workflows/ci.yml && grep -qF "Frontend/package-lock.json" .github/workflows/ci.yml && grep -qF "github.event_name == 'pull_request'" .github/workflows/ci.yml && ! grep -q "secrets\." .github/workflows/ci.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/ci.yml` exists and parses as valid YAML
    - Top-level `jobs:` map contains EXACTLY three keys: `hygiene-check`, `backend-build-test`, `frontend-lint-build`
    - `on.pull_request.branches` equals `[main]` and `on.push.branches` equals `[main]`
    - `concurrency.group` is the literal `${{ github.workflow }}-${{ github.ref }}`
    - `concurrency.cancel-in-progress` is the literal string `${{ github.event_name == 'pull_request' }}` (not a hard-coded `true` or `false`)
    - `backend-build-test` uses `actions/setup-dotnet@v4` with `dotnet-version: '10.0.x'`, `cache: true`, and `cache-dependency-path` listing both `Backend/**/packages.lock.json` and `Backend/Directory.Packages.props`
    - `backend-build-test` runs `dotnet restore`, `dotnet build --no-restore --configuration Release`, and `dotnet test --no-build --configuration Release --verbosity normal` from `working-directory: Backend`
    - `frontend-lint-build` uses `actions/setup-node@v4` with `node-version: '22'`, `cache: 'npm'`, `cache-dependency-path: Frontend/package-lock.json`
    - `frontend-lint-build` runs `npm ci`, `npm run lint`, `npm run build` from `working-directory: Frontend`
    - `hygiene-check` runs the bash script that asserts on `storage`, `Backend/storage`, `Backend/src/TaxReader.Api/storage` directories AND any `build-diag*.txt` / `*.binlog` files
    - File contains no `secrets.*` references (Phase 1 has no required secrets per D-11)
    - File contains no Postgres service container (D-09 defers to Phase 7)
  </acceptance_criteria>
  <done>CI workflow file exists, parses, and the three jobs match the locked decisions. First PR raised against `main` will run all three in parallel with green status against the post-Phase-1 codebase.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create top-level README.md (FND-05)</name>
  <files>
    README.md
  </files>
  <read_first>
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-CONTEXT.md (D-12 — locked README structure: tagline → prerequisites → quick start → links; English; no screenshots)
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-PATTERNS.md (README.md row — closest analog is `Backend/README.md` for tone/format)
    - D:/Programming/Repos/TaxReader/Backend/README.md (analog — read for tone, header style, code-block conventions)
    - D:/Programming/Repos/TaxReader/Frontend/README.md (sibling analog)
    - D:/Programming/Repos/TaxReader/.env.example (the file the README's quick-start instructs the user to copy)
    - D:/Programming/Repos/TaxReader/docker-compose.yml (confirm canonical run command is `docker compose up --build`)
    - D:/Programming/Repos/TaxReader/CLAUDE.md (referenced from README; check it exists)
    - D:/Programming/Repos/TaxReader/.planning/codebase/ (referenced from README; verify directory exists)
    - D:/Programming/Repos/TaxReader/Caddyfile (confirm Caddy serves on `https://localhost` for the post-startup browser URL)
  </read_first>
  <behavior>
    - Test 1 (smoke): `test -f README.md` succeeds — file exists at repo root
    - Test 2: `grep -F "docker compose up --build" README.md` matches at least one line
    - Test 3: `grep -F ".env.example" README.md` matches at least one line
    - Test 4: `grep -F "CLAUDE.md" README.md` matches at least one line
    - Test 5: `grep -F "TaxReader" README.md` matches at least one line in a heading or tagline
    - Test 6: README references all four prerequisites — `.NET 10`, `Node 22`, `Docker`, `Tesseract`
    - Test 7 (manual / smoke): the workflow `cp .env.example .env` → edit secrets → `docker compose up --build` → browser to `https://localhost` succeeds for a new developer on a fresh checkout
  </behavior>
  <action>
    Create `README.md` at the repository root (NEW). Per D-12 — English (matches code/comment language), no screenshots, brief structure. The file content:

    ```markdown
    # TaxReader

    > German tax-receipt aggregator — turn a pile of PDF and image receipts into a clean per-category-per-year expense summary for ELSTER or Steuerberater hand-off.

    Receipts are text-extracted (PdfPig + Tesseract OCR), parsed by format-specific parsers (Amazon, Eduki, Generic), AI-classified into tax-relevant categories, and aggregated into a German-localized PDF/CSV report.

    See [`CLAUDE.md`](CLAUDE.md) for project guidelines and [`.planning/codebase/`](.planning/codebase/) for stack, architecture, and conventions intel.

    ## Prerequisites

    - **.NET 10 SDK** — backend build (`dotnet --version` reports `10.x`)
    - **Node.js 22+** — frontend build (`node --version` reports `v22.x` or higher)
    - **Docker Desktop** with Docker Compose v2 — full stack run (`docker compose version` succeeds)
    - **Tesseract OCR** with `deu+eng` language packs — only required for **non-container** local dev (the `api` container ships Tesseract via apt)
      - macOS: `brew install tesseract tesseract-lang`
      - Linux (Debian/Ubuntu): `sudo apt-get install tesseract-ocr tesseract-ocr-deu tesseract-ocr-eng`
      - Windows: install from <https://github.com/UB-Mannheim/tesseract/wiki>; ensure `tessdata` contains `deu.traineddata` + `eng.traineddata`

    ## Quick Start

    ```bash
    # 1. Clone and enter the repo
    git clone <repo-url> taxreader
    cd taxreader

    # 2. Copy the env template and edit secrets
    cp .env.example .env
    # then open .env in your editor and fill in:
    #   - JWT_SECRET (any long random string for local dev)
    #   - ANTHROPIC_API_KEY (your Anthropic key)
    #   - POSTGRES_PASSWORD (any string)

    # 3. Bring up the full stack
    docker compose up --build

    # 4. Open the app
    # → https://localhost  (Caddy terminates TLS with a self-signed cert on first run)
    ```

    The first build takes a few minutes (Docker pulls .NET 10, Node 22, Tesseract, and Postgres images). Subsequent runs reuse cached layers.

    ## Common tasks

    | Task | Command |
    |------|---------|
    | Backend build | `dotnet build Backend` |
    | Backend tests | `dotnet test Backend` |
    | Backend run (without container) | `dotnet run --project Backend/src/TaxReader.Api` |
    | Frontend dev server | `cd Frontend && npm install && npm run dev` |
    | Frontend production build | `cd Frontend && npm run build` |
    | Reset full stack (drops the Postgres volume) | `docker compose down -v` |
    | Add an EF Core migration | `dotnet ef migrations add <Name> -p Backend/src/TaxReader.Infrastructure -s Backend/src/TaxReader.Api` |

    ## Documentation

    - [`CLAUDE.md`](CLAUDE.md) — project guidelines, conventions, architecture rules
    - [`.planning/PROJECT.md`](.planning/PROJECT.md) — product vision, constraints, key decisions
    - [`.planning/ROADMAP.md`](.planning/ROADMAP.md) — milestone phases and status
    - [`.planning/codebase/`](.planning/codebase/) — stack inventory, architecture, conventions, concerns

    ## License

    See [`LICENSE`](LICENSE) (when added). Pre-commercial — see `.planning/PROJECT.md` for the launch milestone scope.
    ```

    Notes for the executor:
    - English, dev-facing — D-12 confirms the convention boundary: end-user UI is German (`Sie`-form), but dev tooling stays English to match the existing codebase comments.
    - Use plain Markdown (no GitHub-specific extensions beyond the standard table). Code blocks fenced with triple backticks; language tags (`bash`, `markdown`) where helpful.
    - **Do NOT add screenshots** — D-12 explicitly excludes them.
    - **Do NOT add a CI badge** in this task — branch protection isn't enabled yet (manual follow-up). Adding a green badge before the first run is misleading. A follow-up SUMMARY note can call it out.
    - The `LICENSE` link points to a file that does not yet exist — that's intentional; commercial-launch milestone will add the licence text. The link is a forward-pointing placeholder.
    - **Do NOT touch `Backend/README.md` or `Frontend/README.md`** — they remain scoped to their subprojects.
  </action>
  <verify>
    <automated>cd D:/Programming/Repos/TaxReader && test -f README.md && grep -qF "docker compose up --build" README.md && grep -qF ".env.example" README.md && grep -qF "CLAUDE.md" README.md && grep -qF ".NET 10" README.md && grep -qF "Node.js 22" README.md && grep -qF "Tesseract" README.md && grep -qF "https://localhost" README.md && grep -qF "TaxReader" README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `README.md` exists at the repository root (D:/Programming/Repos/TaxReader/README.md)
    - First-line heading is `# TaxReader`
    - Tagline (block-quote starting with `>`) describes the product in one sentence
    - "Prerequisites" section lists `.NET 10`, `Node.js 22`, `Docker Desktop`, and `Tesseract` (with `deu+eng` mention)
    - "Quick Start" section contains the exact command `docker compose up --build` inside a fenced code block
    - "Quick Start" section contains the command `cp .env.example .env`
    - "Quick Start" section references the post-startup URL `https://localhost`
    - At least one section links to `CLAUDE.md` and at least one section links to `.planning/codebase/`
    - File contains no image references (`![...](...)` syntax) — confirming the no-screenshots rule
    - File contains the literal string `TaxReader` (the rebrand-from-BelegPilot is honoured)
    - `Backend/README.md` and `Frontend/README.md` are unchanged
  </acceptance_criteria>
  <done>Top-level README in place; new-developer onboarding documented end-to-end; Phase 1 success criterion #6 satisfied.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3 (manual): Enable branch protection on `main` after first green CI run</name>
  <files>
    (GitHub repository settings — no repo files touched)
  </files>
  <read_first>
    - D:/Programming/Repos/TaxReader/.planning/phases/01-foundation-cleanup-ci/01-CONTEXT.md (D-10 — locked branch-protection spec: PRs required, three required status checks, no required reviewers, no signed-commit requirement, no linear-history requirement)
  </read_first>
  <behavior>
    - Test 1 (manual): after this task, opening any PR against `main` shows three required status checks (`hygiene-check`, `backend-build-test`, `frontend-lint-build`); merge button is disabled until all three pass
    - Test 2 (manual): direct push to `main` (e.g. `git push origin main` from local) is rejected by GitHub with a "branch protection" error
    - Test 3 (manual): after merge, `Settings → Branches → main` page shows the rule active with the three required checks listed
  </behavior>
  <action>
    This task is a manual-only step that runs AFTER the first PR carrying Tasks 1 and 2 merges and the CI workflow has run at least once green. Branch-protection rules can only attach to status checks that have already reported, so the order is mandatory: merge first, then enable.

    Steps:

    1. Wait for the PR that adds `.github/workflows/ci.yml` and `README.md` to merge into `main`.
    2. Wait for the first post-merge run of CI to complete green (so the three job names are registered with GitHub's branch-protection picker).
    3. In the GitHub UI, navigate to: `Settings → Branches → Add branch protection rule`.
    4. Apply the following settings (D-10):
       - **Branch name pattern:** `main`
       - **Require a pull request before merging:** enabled
         - Required approvals: **0** (solo dev — no required reviewers)
         - Dismiss stale pull request approvals when new commits are pushed: leave default
       - **Require status checks to pass before merging:** enabled
         - Require branches to be up to date before merging: enabled
         - Required status checks (search and select all three):
           - `hygiene-check` (or its full job name `Hygiene check (no PII / build artifacts)`)
           - `backend-build-test` (or `Backend build + test`)
           - `frontend-lint-build` (or `Frontend lint + build`)
       - **Require conversation resolution before merging:** leave default (off)
       - **Require signed commits:** OFF (D-10)
       - **Require linear history:** OFF (D-10)
       - **Require deployments to succeed before merging:** OFF
       - **Lock branch:** OFF
       - **Do not allow bypassing the above settings:** ON (so admins also follow the rule unless they explicitly bypass)
       - **Restrict who can push to matching branches:** leave default (only repo collaborators with write access)
    5. Save the rule.
    6. Verify by opening a draft PR with a trivial change (e.g. a comment-only edit) — confirm the three required checks appear and merge is blocked until they pass.
    7. Record the rule's existence in `.planning/phases/01-foundation-cleanup-ci/01-02-SUMMARY.md` (manual paste of the rule details — there's no GitHub API export in scope for this plan).

    **IMPORTANT for the executor agent:** Do NOT attempt to enable branch protection automatically via the GitHub REST API or `gh` CLI in this task. D-10 specifies a one-time human-driven setup; an automated change to repo security settings is out of scope for the executor and may not work without an admin token. Mark this task as manual-pending and instruct the user to perform the steps after merge.
  </action>
  <verify>
    <automated>echo "Manual task — no automated verification possible. Verify in GitHub UI after first PR merge."</automated>
  </verify>
  <acceptance_criteria>
    - The branch-protection rule on `main` is enabled with the three required status checks
    - The rule blocks direct pushes to `main` (PRs required)
    - The rule blocks merges when any of the three checks fail
    - "Required reviewers" is set to 0 (solo dev)
    - Signed-commit requirement is OFF; linear-history requirement is OFF
    - Rule details recorded in `01-02-SUMMARY.md` for audit trail
  </acceptance_criteria>
  <done>Branch protection active; success criterion #1 ("Every PR has merge-blocking checks") fully satisfied — the workflow file alone makes the checks RUN, but branch protection makes them BLOCKING.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → CI runner | Untrusted PR contributors can submit code that the runner executes (build, test). GitHub's default `pull_request` trigger uses the BASE branch's workflow, mitigating most code-execution risks; default `GITHUB_TOKEN` permissions are read-only for `pull_request` from forks. |
| CI runner → external networks | `dotnet restore` and `npm ci` reach out to nuget.org and registry.npmjs.org. No other outbound traffic in this workflow. |
| Repository → main branch | Branch protection (Task 3) gates direct push and unverified merges. |
| Working tree → docker image | Hygiene-check job verifies no PII PDFs / `build-diag*.txt` enter `main` via PR. |

## STRIDE Threat Register (ASVS L1 — V12 Files & Resources, V14 Configuration)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure (V12.5) | Leaked PII (receipt PDFs) re-introduced via PR | mitigate | `hygiene-check` job fails the build if `Backend/src/TaxReader.Api/storage/` (or any of the other forbidden paths) reappears in the working tree. Branch protection (Task 3) makes the check merge-blocking. |
| T-01-02-02 | Information Disclosure (V12.5) | Build artifacts (`build-diag*.txt`, `*.binlog`) committed to the tree | mitigate | Same `hygiene-check` job catches these via `find` glob; `.gitignore` rule from Plan 01-01 prevents accidental staging. |
| T-01-02-03 | Tampering / Elevation of Privilege | Malicious PR contributor adds a CI step to leak secrets | mitigate | D-11: no Phase 1 secrets in CI. The workflow has no `${{ secrets.* }}` references. Future plans add secrets only when warranted, gated by code review. |
| T-01-02-04 | Tampering | Direct push to `main` bypasses CI | mitigate | Task 3's branch protection blocks direct push. Until Task 3 is enabled, this is a known gap — solo dev mitigation: developer discipline + the very first CI workflow run on `main` post-merge sets the baseline. |
| T-01-02-05 | Information Disclosure | README.md leaks production-only env-var values or secrets | mitigate | README only references `.env.example` placeholders; no real DSNs / keys / passwords are inlined. The example file commits no secrets (Plan 01-01 + 01-03 update it with empty placeholders only). |
| T-01-02-06 | Denial of Service (V14.4) | Dependent action versions silently change behaviour or are revoked | mitigate | All actions pinned to a major version (`@v4`). Tighter pinning to a SHA is overkill for solo-dev velocity (CONTEXT.md `<deferred>`). Re-evaluate at QA-06 (Phase 7) when alert tuning lands. |
| T-01-02-07 | Tampering (V14.2.1) | Cache invalidation fails for CPM version bumps; CI restores stale packages and tests pass against wrong dependency (RESEARCH.md Pitfall 5) | mitigate | `cache-dependency-path` includes `Backend/Directory.Packages.props`. Verify on first version-bump PR; if broken, follow the lock-file fallback (RESEARCH.md). |
| T-01-02-08 | Repudiation | CI run logs lost when no log retention is configured | accept | GitHub Actions retains run logs for 90 days by default (free tier). Sufficient for solo-dev incident response. Re-evaluate at scale. |

**Out of scope (V2 Authentication / V3 Session / V4 Access / V5 Input / V6 Crypto / V8 Data Protection):** This plan does not touch authentication, session management, access control, input validation, cryptography, or runtime data handling. Those concerns live in their respective phases.

</threat_model>

<verification>
After all three tasks complete (Task 3 = manual), run from `D:/Programming/Repos/TaxReader/`:

```bash
# Workflow file presence + structure
test -f .github/workflows/ci.yml
python -c "import yaml; cfg=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=set(cfg['jobs'].keys()); assert jobs == {'hygiene-check','backend-build-test','frontend-lint-build'}; print('OK')"

# README presence + key strings
test -f README.md
grep -qF "docker compose up --build" README.md
grep -qF ".env.example" README.md
grep -qF "CLAUDE.md" README.md
grep -qF "https://localhost" README.md

# Smoke: hygiene-check passes against the current tree (Plan 01-01 already ran, so storage/ + build-diag.txt should be absent)
bash -c '
  set -e
  for path in "storage" "Backend/storage" "Backend/src/TaxReader.Api/storage"; do
    if [ -d "$path" ]; then echo "FAIL: $path exists"; exit 1; fi
  done
  if find . -type f \( -name "build-diag*.txt" -o -name "*.binlog" \) -not -path "./.git/*" 2>/dev/null | grep -q .; then
    echo "FAIL: build-diag or binlog found"; exit 1;
  fi
  echo "OK"
'

# Build smoke (sanity that the local tree compiles before pushing the workflow file)
dotnet build Backend --configuration Release
cd Frontend && npm ci && npm run lint && npm run build && cd ..
```

After the first PR merges and CI runs:
- Confirm in GitHub Actions tab that all three jobs ran in parallel and exited green.
- Verify the duration is reasonable (< 6 minutes total) — anything dramatically longer signals a cache miss worth investigating.
- Proceed with Task 3 (manual branch-protection setup).
</verification>

<success_criteria>
- `.github/workflows/ci.yml` exists, parses as valid YAML, and triggers on `pull_request` to `main` and `push` to `main`
- Three jobs (`hygiene-check`, `backend-build-test`, `frontend-lint-build`) run in parallel with the locked decisions from D-08 (concurrency, cancel-in-progress for PRs only)
- `hygiene-check` fails on any of the five forbidden patterns (`storage/`, `Backend/storage/`, `Backend/src/TaxReader.Api/storage/`, `build-diag*.txt`, `*.binlog`)
- `backend-build-test` runs `dotnet restore` + `dotnet build --configuration Release` + `dotnet test --configuration Release` against `Backend/`, with cache key including `Backend/Directory.Packages.props`
- `frontend-lint-build` runs `npm ci` + `npm run lint` + `npm run build` against `Frontend/`
- No CI secrets are referenced (D-11)
- No Postgres service container is provisioned (D-09 — Phase 7 territory)
- `README.md` exists at repo root, English, with prerequisites + quick-start + documentation links
- README references `docker compose up --build`, `cp .env.example .env`, `https://localhost`, `CLAUDE.md`, `.planning/codebase/`
- Phase 1 ROADMAP success criteria #1, #2 (CI half), and #6 are satisfied
- Branch protection rule is recorded as a pending manual step (Task 3) with exact configuration from D-10
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cleanup-ci/01-02-SUMMARY.md` recording:
- First-PR CI run URL (for audit trail)
- Whether the CPM cache-dependency-path strategy worked or if a lock-file fallback was needed (RESEARCH.md Pitfall 5)
- Branch-protection rule status: pending (Task 3) / enabled / deviation
- Any deviations from plan + reason
- Notes on actual CI duration (cold + warm cache) for future reference
</output>
