---
phase: 07-test-depth-launch-qa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/tests/TaxReader.UnitTests/Services/AuthServiceTests.cs
  - Backend/tests/TaxReader.UnitTests/Services/TokenServiceTests.cs
  - Backend/tests/TaxReader.UnitTests/Services/AiOnlyClassificationServiceTests.cs
autonomous: true
requirements: [QA-01]
user_setup: []

must_haves:
  truths:
    - "AuthService.RegisterAsync rejects a duplicate email with the exact German error and a valid registration succeeds with a hashed password"
    - "AuthService.LoginAsync returns the German 'Ungültige E-Mail oder Passwort.' on a bad password and succeeds on a correct BCrypt-verified password"
    - "TokenService pre-charge (TryConsumeManyAsync) returns false when the balance is insufficient and writes Consumption ledger rows when sufficient; RefundManyAsync writes Refund rows"
    - "AiOnlyClassificationService refunds tokens and returns Unknown when AI fails, refunds the per-item entry when the AI returns Unbekannt, and applies the auto-confirm threshold"
  artifacts:
    - path: "Backend/tests/TaxReader.UnitTests/Services/AuthServiceTests.cs"
      provides: "Register/login/BCrypt-verify/refresh coverage for the highest-risk untested auth service"
      contains: "Ungültige E-Mail oder Passwort."
    - path: "Backend/tests/TaxReader.UnitTests/Services/TokenServiceTests.cs"
      provides: "Atomic ledger consume/refund/add coverage"
      contains: "TryConsumeManyAsync"
    - path: "Backend/tests/TaxReader.UnitTests/Services/AiOnlyClassificationServiceTests.cs"
      provides: "Token pre-charge, refund-on-Unknown, refund-on-failure, auto-confirm threshold coverage"
      contains: "RefundManyAsync"
  key_links:
    - from: "AuthServiceTests.cs"
      to: "AuthService"
      via: "constructor-as-setup with in-memory AppDbContext + Mock<IRefreshTokenService>"
      pattern: "new AuthService"
    - from: "AiOnlyClassificationServiceTests.cs"
      to: "AiOnlyClassificationService"
      via: "Mock<IAiClassifier> + Mock<ITokenService>"
      pattern: "Mock<IAiClassifier>"
---

<objective>
Backfill dedicated unit tests for the three highest-risk currently-untested money/security services (D-01): `AuthService`, `TokenService`, `AiOnlyClassificationService`. These guard Core Value (trustworthy classification) and revenue, and today have no dedicated test files (only indirect `AuthService.LoginAsync` coverage via `IsAdminClaimTests`).

Purpose: Catch regressions in registration/login/BCrypt-verify, the atomic token ledger, and the AI pre-charge/refund/threshold logic before launch.
Output: Three new unit-test classes in the existing `TaxReader.UnitTests` project, fast and in-memory, runnable on every PR.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-test-depth-launch-qa/07-PATTERNS.md

<interfaces>
<!-- Real signatures from the codebase. Match these exactly. -->

In-memory test-DB setup (Auth/RefreshTokenServiceTests.cs:26-64 — replicate):
```csharp
var options = new DbContextOptionsBuilder<AppDbContext>()
    .UseInMemoryDatabase(Guid.NewGuid().ToString()).Options;   // fresh DB per test instance
_dbContext = new AppDbContext(options);
// seed + SaveChanges()
public void Dispose() => _dbContext.Dispose();
```
Use `NullLogger<T>.Instance` for loggers. Seed users via TestDataFactory.CreateRegularUser (BCrypt hash for "test-password-1234").

AuthService.cs signatures: RegisterAsync(RegisterRequest, userAgent, ipAddress, ct), LoginAsync(LoginRequest, userAgent, ipAddress, ct), RefreshAsync(refreshToken, userAgent, ipAddress, ct) — all return Result<AuthResponse>. Mock IRefreshTokenService so register/login don't hit the rotation path. EXACT German strings (AuthService.cs:36,39,101):
- "Ein Konto mit dieser E-Mail existiert bereits."
- "Das Passwort muss mindestens 8 Zeichen lang sein."
- "Ungültige E-Mail oder Passwort."

TokenService.cs signatures: primary ctor TokenService(IAppDbContext dbContext, ICurrentUser currentUser) — MOCK ICurrentUser to return a fixed UserId.
- TryConsumeManyAsync(IReadOnlyList<TokenLedgerEntry>, ct) → bool; false when balance.Balance < total (TokenService.cs:63)
- RefundManyAsync(entries, ct) → UserTokenBalance (TokenService.cs:94)
- AddTokensAsync(amount, type, description, ct) — throws ArgumentOutOfRangeException when amount <= 0 (TokenService.cs:137-138)
- GetOrCreateBalanceAsync seeds InitialFreeTokens = 10 + a "Welcome bonus" TokenTransaction (TokenService.cs:27-47)
Each consume writes a Consumption row with Amount = -entry.Amount + running BalanceAfter; refund writes Refund rows.

AiOnlyClassificationService.cs (ctor :21-27): IAiClassifier, ITokenService, IAppDbContext, IOptions<AnthropicOptions>, ILogger<…>. Method: ClassifyItemsAsync(IEnumerable<ReceiptItem>, Guid userId, ct). Branches (mock IAiClassifier + ITokenService):
- Insufficient tokens: tokenService.TryConsumeManyAsync returns false → every item Unknown, reason "Keine Tokens verfügbar – bitte Credits aufladen." (:57-65)
- Refund-on-Unknown: AI returns Category.Unbekannt → that entry added to refunds, RefundManyAsync called (:90-98,121-122)
- Refund-on-failure: ClassifyBatchAsync throws → RefundManyAsync(ledgerEntries) + all Unknown with "AI-Fehler: …" (:73-78)
- Auto-confirm threshold: seed User.AutoConfirmThreshold; result.Confidence >= threshold → ClassificationStatus.Confirmed + reason prefix "Auto-bestätigt (… ≥ …)"; else Suggested (:100-118)
- IsConfigured=false → Unknown with "AI-Klassifizierung nicht konfiguriert." (:38-42)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthServiceTests + TokenServiceTests</name>
  <behavior>
    AuthService:
    - RegisterAsync_NewEmail_ReturnsSuccessWithHashedPassword
    - RegisterAsync_DuplicateEmail_ReturnsFailure ("Ein Konto mit dieser E-Mail existiert bereits.")
    - RegisterAsync_ShortPassword_ReturnsFailure ("Das Passwort muss mindestens 8 Zeichen lang sein.")
    - LoginAsync_WrongPassword_ReturnsFailure ("Ungültige E-Mail oder Passwort.")
    - LoginAsync_CorrectPassword_ReturnsSuccess (BCrypt verify path)
    - LoginAsync_UnknownEmail_ReturnsFailure (same German string — no user enumeration)
    TokenService:
    - TryConsumeManyAsync_InsufficientBalance_ReturnsFalse
    - TryConsumeManyAsync_SufficientBalance_WritesConsumptionRows
    - RefundManyAsync_WritesRefundRows
    - AddTokensAsync_NonPositiveAmount_ThrowsArgumentOutOfRange
    - GetOrCreateBalanceAsync_NewUser_SeedsTenFreeTokensAndWelcomeBonus
  </behavior>
  <read_first>
    - Backend/tests/TaxReader.UnitTests/Auth/RefreshTokenServiceTests.cs (constructor-as-setup + in-memory + Moq + Dispose shape)
    - Backend/tests/TaxReader.UnitTests/Helpers/TestDataFactory.cs (CreateRegularUser BCrypt seed)
    - Backend/src/TaxReader.Infrastructure/Services/AuthService.cs (exact German strings + Result<T> branches)
    - Backend/src/TaxReader.Infrastructure/Services/TokenService.cs (ledger semantics, ctor, throw paths)
    - Backend/src/TaxReader.Application/Interfaces/ (IRefreshTokenService, ICurrentUser signatures)
  </read_first>
  <action>
    Create `Backend/tests/TaxReader.UnitTests/Services/AuthServiceTests.cs`. Constructor-as-setup with `UseInMemoryDatabase(Guid.NewGuid().ToString())`, `Mock<IRefreshTokenService>` (set up IssueAsync/ValidateAndRotateAsync to return a benign success so register/login complete), `Options.Create(new JwtOptions { Secret = "test-secret-test-secret-test-secret-1234", Issuer = "test", Audience = "test", ... })`, and a `Mock<IAuditLogger>` if the ctor requires it (confirm from AuthService.cs). Seed a known user via `TestDataFactory.CreateRegularUser` for login tests. Assert on `result.IsSuccess`/`result.Error` — never catch exceptions. Use the EXACT German strings from the interfaces block. `IDisposable.Dispose() => _dbContext.Dispose()`.

    Create `Backend/tests/TaxReader.UnitTests/Services/TokenServiceTests.cs`. Same in-memory shape; `Mock<ICurrentUser>` returning a fixed `UserId`. Cover the ledger behaviors above. For the consume test, assert a `Consumption` TokenTransaction row exists with `Amount == -entry.Amount` and the balance decremented; for refund, assert `Refund` rows; for `AddTokensAsync(0|-1, …)` assert `await act.Should().ThrowAsync<ArgumentOutOfRangeException>()`; for `GetOrCreateBalanceAsync` on a new user assert Balance == 10 and a "Welcome bonus" transaction exists.
  </action>
  <verify>
    <automated>cd Backend && dotnet test tests/TaxReader.UnitTests/TaxReader.UnitTests.csproj --configuration Release --filter "FullyQualifiedName~AuthServiceTests|FullyQualifiedName~TokenServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AuthServiceTests.cs` contains the exact string `Ungültige E-Mail oder Passwort.` and `Ein Konto mit dieser E-Mail existiert bereits.`
    - `TokenServiceTests.cs` contains `TryConsumeManyAsync`, `RefundManyAsync`, and `ThrowAsync<ArgumentOutOfRangeException>`
    - Both classes use `UseInMemoryDatabase` and `Mock<` collaborators (no real DB)
    - Test names follow `Method_Scenario_Result`
    - The filtered `dotnet test` exits 0
  </acceptance_criteria>
  <done>AuthService register/login/BCrypt-verify and TokenService ledger operations are covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiOnlyClassificationServiceTests</name>
  <behavior>
    - ClassifyItemsAsync_AiNotConfigured_ReturnsUnknownWithGermanReason ("AI-Klassifizierung nicht konfiguriert.")
    - ClassifyItemsAsync_InsufficientTokens_ReturnsUnknownAndDoesNotCallAi ("Keine Tokens verfügbar – bitte Credits aufladen.")
    - ClassifyItemsAsync_AiReturnsUnbekannt_RefundsThatItem (RefundManyAsync called with the Unknown item's entry)
    - ClassifyItemsAsync_AiThrows_RefundsAllAndReturnsUnknown (RefundManyAsync(ledgerEntries) + "AI-Fehler: …")
    - ClassifyItemsAsync_ConfidenceAboveThreshold_MarksConfirmed (reason prefix "Auto-bestätigt")
    - ClassifyItemsAsync_ConfidenceBelowThreshold_MarksSuggested
  </behavior>
  <read_first>
    - Backend/src/TaxReader.Infrastructure/Services/AiOnlyClassificationService.cs (all branches + exact German strings + ctor)
    - Backend/tests/TaxReader.UnitTests/Services/RuleBasedClassifierTests.cs (classifier test shape)
    - Backend/tests/TaxReader.UnitTests/Auth/RefreshTokenServiceTests.cs (Moq usage)
    - Backend/src/TaxReader.Application/Interfaces/ (IAiClassifier, ITokenService signatures; AnthropicOptions)
  </read_first>
  <action>
    Create `Backend/tests/TaxReader.UnitTests/Services/AiOnlyClassificationServiceTests.cs`. Construct the service with `Mock<IAiClassifier>`, `Mock<ITokenService>`, an in-memory `AppDbContext` (seed a User with a known `AutoConfirmThreshold` for the threshold tests), `Options.Create(new AnthropicOptions { ApiKey = "..." })`, and `NullLogger<AiOnlyClassificationService>.Instance`. Drive each branch by configuring the mocks:
    - For not-configured: set up `IAiClassifier.IsConfigured` to return false (or AnthropicOptions without ApiKey, per the real check at :38) → assert all items Unknown + "AI-Klassifizierung nicht konfiguriert."
    - For insufficient tokens: `tokenService.TryConsumeManyAsync(...) => false` → assert all Unknown + "Keine Tokens verfügbar – bitte Credits aufladen." and `Verify(IAiClassifier.ClassifyBatchAsync, Times.Never)`.
    - For refund-on-Unknown: AI batch returns `Category.Unbekannt` for one item → `Verify(tokenService.RefundManyAsync, Times.Once)` containing that item's entry.
    - For refund-on-failure: `ClassifyBatchAsync` throws → `Verify(RefundManyAsync(ledgerEntries), Times.Once)` + items Unknown with "AI-Fehler:".
    - For threshold: AI returns Confidence above/below the seeded `AutoConfirmThreshold` → assert `ClassificationStatus.Confirmed` (reason starts "Auto-bestätigt") vs `Suggested`.
    Confirm against the live DI whether `AiOnlyClassificationService` is still registered directly or wrapped by `HybridClassificationService` (RESEARCH Open Q 3) — test the class directly regardless (file is present and verified).
  </action>
  <verify>
    <automated>cd Backend && dotnet test tests/TaxReader.UnitTests/TaxReader.UnitTests.csproj --configuration Release --filter "FullyQualifiedName~AiOnlyClassificationServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `AiOnlyClassificationServiceTests.cs` contains `Keine Tokens verfügbar – bitte Credits aufladen.` and `Auto-bestätigt`
    - At least one test uses `Verify(` with `Times.Never` (no AI call when out of tokens) and one with `RefundManyAsync`
    - Service constructed with `Mock<IAiClassifier>` and `Mock<ITokenService>` (no real Anthropic call)
    - The filtered `dotnet test` exits 0
  </acceptance_criteria>
  <done>All four D-01 AiOnlyClassificationService behaviors (pre-charge, refund-on-Unknown, refund-on-failure, auto-confirm threshold) plus the not-configured path are covered and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client credentials → AuthService | Untrusted email/password; BCrypt verify + non-enumerating failure copy must hold |
| Token ledger mutations → TokenService | Pre-charge/refund must be atomic so a failed AI call cannot drain or double-refund balance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Spoofing | AuthService.LoginAsync | mitigate | Test asserts both wrong-password and unknown-email return the SAME German string ("Ungültige E-Mail oder Passwort.") — no user enumeration (ASVS V2) |
| T-07-06 | Tampering | TokenService ledger | mitigate | TokenServiceTests asserts TryConsumeManyAsync returns false on insufficient balance (no over-spend) and RefundManyAsync writes Refund rows (no silent loss) |
| T-07-07 | Repudiation | AiOnlyClassificationService refund path | mitigate | Tests assert refund-on-failure and refund-on-Unknown fire, so a charged token is always reconciled — protects revenue accounting |
| T-07-08 | Information Disclosure | test JWT secret / Anthropic key | accept | Test-only placeholder secrets; never reach production |
</threat_model>

<verification>
- `dotnet test Backend/tests/TaxReader.UnitTests --filter "FullyQualifiedName~ServiceTests"` is green and fast (in-memory).
- The three target services now have dedicated test files (the RESEARCH "still true" gap is closed).
</verification>

<success_criteria>
- AuthServiceTests, TokenServiceTests, AiOnlyClassificationServiceTests exist in TaxReader.UnitTests and pass.
- German error strings asserted verbatim; no exceptions caught for control flow (Result<T> pattern).
- These run in the fast suite on every PR (D-04 — no Docker needed).
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-depth-launch-qa/07-02-SUMMARY.md`
</output>
