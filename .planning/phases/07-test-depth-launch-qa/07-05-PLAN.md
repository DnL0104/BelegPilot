---
phase: 07-test-depth-launch-qa
plan: 05
type: execute
wave: 2
depends_on: [07-04]
files_modified:
  - Frontend/package.json
  - Frontend/playwright.config.ts
  - Frontend/e2e/happy-path.spec.ts
  - Frontend/.gitignore
autonomous: true
requirements: [QA-03, QA-05]
user_setup: []

must_haves:
  truths:
    - "Playwright runs the happy path (register → login → upload → see-classification → confirm → report → export) end-to-end against the standalone Next.js production server in DE locale"
    - "The E2E suite runs against `next build && next start` (standalone production), NOT `next dev`"
    - "sm (640px) and md (768px) viewport projects run the responsive smoke for the automated QA-05 portion"
    - "The browser locale is de-DE and timezone Europe/Berlin so German copy and EUR formatting are exercised"
  artifacts:
    - path: "Frontend/playwright.config.ts"
      provides: "Playwright 1.50 config — DE locale, webServer standalone, sm/md viewport projects"
      contains: "locale: 'de-DE'"
    - path: "Frontend/e2e/happy-path.spec.ts"
      provides: "End-to-end happy-path spec across the real routes"
      contains: "register"
  key_links:
    - from: "playwright.config.ts"
      to: "next start"
      via: "webServer command 'npm run build && npm run start'"
      pattern: "webServer"
    - from: "happy-path.spec.ts"
      to: "the real app routes"
      via: "page.goto('/register') → /login → /upload → /receipts/[id] → /reports → export"
      pattern: "/register"
---

<objective>
Add Playwright 1.50 to the frontend (no E2E today) and write the QA-03 DE-locale happy-path spec — register → login → upload-receipt → see-classification → confirm → see-report → export — running against the standalone Next.js production server. Ride the automated QA-05 responsive smoke on `sm` (640px) and `md` (768px) viewport projects.

Purpose: First end-to-end coverage of the real user journey in German locale; catch build-only and cross-page-wiring breaks the unit suite cannot. The phone-camera photo-upload portion of QA-05 stays a HUMAN-UAT item (no real-device camera in CI).
Output: `playwright.config.ts` + `e2e/happy-path.spec.ts`, runnable via `npx playwright test`.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-test-depth-launch-qa/07-RESEARCH.md
@.planning/phases/07-test-depth-launch-qa/07-PATTERNS.md

<interfaces>
<!-- CRITICAL: READ Frontend/node_modules/next/dist/docs/01-app/02-guides/testing/playwright.md BEFORE
     writing config (Frontend/AGENTS.md: "This is NOT the Next.js you know"). Authoritative over training data. -->

Install (run in Frontend/):
```
npm install -D @playwright/test@1.50
npx playwright install --with-deps   # browser binaries; required in CI heavy job
```

playwright.config.ts (RESEARCH Pattern 5 — literal):
```ts
import { defineConfig, devices } from '@playwright/test'
export default defineConfig({
  testDir: './e2e',
  use: { baseURL: 'http://localhost:3000', locale: 'de-DE', timezoneId: 'Europe/Berlin' },
  projects: [
    { name: 'desktop', use: { ...devices['Desktop Chrome'] } },
    { name: 'md', use: { viewport: { width: 768, height: 1024 } } },   // QA-05
    { name: 'sm', use: { viewport: { width: 640, height: 900 } } },    // QA-05
  ],
  webServer: {
    command: 'npm run build && npm run start',   // standalone production server, NOT next dev
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
})
```
Note: `npm run start` is `next start --hostname 0.0.0.0` (package.json) — serves on :3000. Add `e2e/` test-results + `playwright-report/` + `/test-results/` to Frontend/.gitignore.

REAL routes (verified present — the happy path):
- `/register` (app/register/page.tsx — useState form, native validation)
- `/login` (app/login/page.tsx)
- `/(authenticated)/upload` → upload-form.tsx (FileDropzone)
- `/(authenticated)/receipts` list → `/(authenticated)/receipts/[id]` detail (classify-dialog.tsx confirm)
- `/(authenticated)/reports` → export-buttons.tsx (PDF/CSV export)
All copy is German (Sie-form). The backend must be reachable for a true end-to-end run — the Next.js rewrite proxies `/api/v1/*` to BACKEND_API_URL.
</interfaces>

<dependency_note>
A real end-to-end happy path needs the BACKEND running and reachable (Next rewrites `/api/v1/*` → backend). In CI this is the heavy job (07-06 wires the full stack / docker compose for the run). LOCALLY, the executor runs the backend (e.g. `docker compose up` or `dotnet run`) before `npx playwright test`, OR writes the spec to be resilient and documents the backend prerequisite in the spec header. Do NOT mock the API for the happy path — it must exercise the real stack (RESEARCH Architectural Responsibility Map: Playwright = full stack).
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright install + config (DE locale, standalone webServer, sm/md projects)</name>
  <read_first>
    - Frontend/AGENTS.md
    - Frontend/node_modules/next/dist/docs/01-app/02-guides/testing/playwright.md (authoritative — READ before config)
    - Frontend/package.json (start script + to extend with an e2e script)
    - Frontend/next.config.ts (the /api/v1/* rewrite to the backend — confirms the proxy path)
    - Frontend/.gitignore (append playwright artifacts)
  </read_first>
  <action>
    Install per the interfaces block in Frontend/. Create `Frontend/playwright.config.ts` exactly per RESEARCH Pattern 5 (read the bundled playwright.md first to confirm no Next.js-16 deltas). Add `"test:e2e": "playwright test"` to package.json scripts. Append to `Frontend/.gitignore`: `/test-results/`, `/playwright-report/`, `/playwright/.cache/`. Ensure `testDir: './e2e'` so Vitest's `exclude: ['**/e2e/**']` (07-04) and Playwright's testDir do not overlap (RESEARCH anti-pattern).
  </action>
  <verify>
    <automated>cd Frontend && npx playwright test --list</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains `locale: 'de-DE'`, `timezoneId: 'Europe/Berlin'`, a `webServer` with `npm run build && npm run start`, and `sm`/`md` viewport projects
    - `package.json` scripts contain `"test:e2e"`
    - `Frontend/.gitignore` contains `playwright-report`
    - `npx playwright test --list` runs without a config error (lists specs across the projects)
  </acceptance_criteria>
  <done>Playwright is installed and configured against the standalone production server in DE locale with sm/md viewport projects.</done>
</task>

<task type="auto">
  <name>Task 2: e2e/happy-path.spec.ts (register → … → export) in DE locale</name>
  <read_first>
    - Frontend/src/app/register/page.tsx (field names, submit button German label)
    - Frontend/src/app/login/page.tsx (login form labels)
    - Frontend/src/components/upload/upload-form.tsx (upload control + German copy)
    - Frontend/src/components/receipts/classify-dialog.tsx (confirm button text "Klassifizierung bestätigt" path)
    - Frontend/src/components/reports/export-buttons.tsx (export trigger labels)
    - Frontend/node_modules/next/dist/docs/01-app/02-guides/testing/playwright.md (spec idioms)
  </read_first>
  <action>
    Create `Frontend/e2e/happy-path.spec.ts`. Header comment: documents the backend prerequisite (the real stack must be up; in CI the heavy job provides it). Write ONE `test('happy path: register → upload → classify → confirm → report → export', ...)` that:
    1. `page.goto('/register')`; fill a unique email (e.g. `playwright+${Date.now()}@example.test`) + password ≥ 8 chars; submit; expect navigation to an authenticated route or login.
    2. If routed to `/login`, log in with the same credentials.
    3. `page.goto('/upload')` (or click the German nav link); upload a small fixture PDF from `e2e/fixtures/` (create a tiny sample receipt PDF fixture, or reuse one from the repo's test assets if present — check Backend test assets first); expect the upload success state (202/queued German copy).
    4. Navigate to receipts, open a receipt detail; assert a classification/suggestion is visible (German label via categoryLabel).
    5. Confirm a classification via the classify-dialog; assert the German confirmation toast/state ("Klassifizierung bestätigt" or "Vorschlag bestätigt").
    6. `page.goto('/reports')`; assert a category total renders with EUR de-DE formatting (e.g. text matching `/\d+,\d{2}\s*€/` or the `€` symbol).
    7. Trigger an export (PDF or CSV) via export-buttons; assert the download starts (`page.waitForEvent('download')`) or the export request succeeds.
    Use `getByRole`/`getByText` with German strings. Locators must match the REAL labels in the components above — read each before asserting. Keep selectors resilient (prefer roles/labels over brittle CSS).

    If a full end-to-end run is not feasible in the executor's local env (no backend), still author the complete spec and verify it via `npx playwright test --list` + a dry compile; mark the live green run as the heavy-CI / local-stack gate. Do NOT weaken the spec into API mocks.
  </action>
  <verify>
    <automated>cd Frontend && npx playwright test --list e2e/happy-path.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/happy-path.spec.ts` contains `/register`, an upload step, a classify-confirm step, a reports assertion, and an export step in a single ordered test
    - The spec asserts German UI copy and de-DE EUR formatting (contains `€` or a `,\d{2}` money pattern)
    - The spec does NOT mock `/api/v1/*` (exercises the real stack)
    - `npx playwright test --list` discovers the spec across desktop/md/sm projects without a config/parse error
  </acceptance_criteria>
  <done>The DE-locale happy-path spec exists, covers the full register→export journey against the real stack, and lists/parses cleanly; the live green run is the heavy-CI / local-stack gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test user → full stack (frontend → API → DB) | The happy path exercises real auth + per-user data scoping end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Elevation (cross-user data) | E2E authenticated routes | mitigate | The happy path registers a fresh user and only sees its own receipts/reports — a regression that leaked another user's data would surface as unexpected rows; per-user scoping is exercised end-to-end (ASVS V4) |
| T-07-16 | Information Disclosure | test credentials in spec | accept | Throwaway unique emails + test passwords against an ephemeral/test stack; never real user data |
| T-07-17 | Tampering | export download integrity | accept | Export trigger asserted for success only; format correctness covered by backend export tests (out of QA-03 scope) |
</threat_model>

<verification>
- `npx playwright test --list` discovers the happy path across desktop/sm/md projects.
- With the full stack up (local docker compose or CI heavy job), `npx playwright test` is green — this is wired into CI by 07-06 and is a D-05 hard launch blocker.
</verification>

<success_criteria>
- Playwright 1.50 installed; config runs against the standalone production server in de-DE locale with sm/md viewport projects (automated QA-05 portion).
- The happy-path spec covers register → login → upload → classify → confirm → report → export against the real stack with German-copy + EUR assertions.
- Phone-camera photo-upload (QA-05) is explicitly deferred to HUMAN-UAT (captured in 07-07).
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-depth-launch-qa/07-05-SUMMARY.md`
</output>
