---
phase: 07-test-depth-launch-qa
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/src/TaxReader.Api/Endpoints/HealthEndpoints.cs
  - Backend/src/TaxReader.Api/Program.cs
  - Backend/tests/TaxReader.UnitTests/Health/HealthEndpointTests.cs
autonomous: true
requirements: [OBS-03]
user_setup: []

must_haves:
  truths:
    - "GET /health returns 200 + JSON containing \"healthy\" when the DB is reachable, and is anonymous (no JWT required)"
    - "GET /api/v1/health returns 200 + JSON reporting DB and Anthropic-configured status, and is anonymous"
    - "Both health endpoints return JSON that contains NO connection string, secret, Anthropic key, or stack trace — only coarse status fields"
    - "When a component is unhealthy the endpoint returns 503 so a status-code monitor also catches it"
  artifacts:
    - path: "Backend/src/TaxReader.Api/Endpoints/HealthEndpoints.cs"
      provides: "/health (DB ping) + /api/v1/health (DB + Anthropic config) anonymous endpoints returning minimal JSON"
      contains: "AllowAnonymous"
    - path: "Backend/tests/TaxReader.UnitTests/Health/HealthEndpointTests.cs"
      provides: "WAF tests asserting 200/JSON, anonymity, and no-secret-leak"
      contains: "healthy"
  key_links:
    - from: "Program.cs"
      to: "HealthEndpoints.MapHealthEndpoints"
      via: "app.MapHealthEndpoints() near app.MapStripeWebhookEndpoint() (anonymous, outside auth group)"
      pattern: "MapHealthEndpoints"
    - from: "HealthEndpoints.cs"
      to: "IAppDbContext.Database.CanConnectAsync"
      via: "DB ping"
      pattern: "CanConnectAsync"
---

<objective>
Build the two health endpoints required by OBS-03 — `/health` (liveness + DB ping) and `/api/v1/health` (DB ping + Anthropic-configured check) — as anonymous endpoints returning a minimal JSON body so BetterStack can run a keyword monitor on `"healthy"`. They do not exist yet and must be built fresh, leaking no secrets.

Purpose: BetterStack (wired in 07-07) probes these unauthenticated; a status-code-only monitor misses real degradation (RESEARCH Pitfall 6), so the JSON body must reflect component health. Information-disclosure is a tracked threat — the body must contain only coarse status.
Output: `HealthEndpoints.cs` + Program.cs wiring + WAF tests.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-test-depth-launch-qa/07-RESEARCH.md
@.planning/phases/07-test-depth-launch-qa/07-PATTERNS.md

<interfaces>
<!-- Real signatures/wiring from the codebase. -->

Endpoint pattern (Backend/src/TaxReader.Api/Endpoints/AuthEndpoints.cs): static `MapXxxEndpoints` extension; `.AllowAnonymous()` opts out of the global auth requirement (AuthEndpoints.cs:34); `.WithName`/`.WithSummary`/`.WithTags`.

Program.cs wiring point (Program.cs:352-372):
```csharp
var api = app.MapGroup("/api/v1").RequireAuthorization();   // line 354 — auth by default
api.MapAuthEndpoints();
// ... other groups ...
app.MapStripeWebhookEndpoint();   // line 368 — anonymous, top-level, OUTSIDE the auth group
```
- `/health` → register on `app` directly (top-level, no version prefix), like MapStripeWebhookEndpoint — anonymous.
- `/api/v1/health` → register inside the `api` group BUT with `.AllowAnonymous()`.

DB ping: `IAppDbContext` exposes `Database` — use `await dbContext.Database.CanConnectAsync(ct)`. (IAppDbContext is implemented by AppDbContext; registered Scoped.)

Anthropic-configured check: `IAiClassifier.IsConfigured` (bool) is the property used in AiOnlyClassificationService.cs:38. Inject `IAiClassifier` and read `.IsConfigured`. Do NOT read or echo the API key.

WAF test harness: use Helpers/RateLimitTestFactory.BuildFactory() (in-memory DB, all boot settings) — health endpoints work against it. Put the test class in collection `[Collection(RateLimiterTestCollection.Name)]` (DisableParallelization) since it spins a WAF host.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HealthEndpoints.cs (/health + /api/v1/health) + Program.cs wiring</name>
  <read_first>
    - Backend/src/TaxReader.Api/Endpoints/AuthEndpoints.cs (MapXxxEndpoints + AllowAnonymous shape)
    - Backend/src/TaxReader.Api/Endpoints/TokenEndpoints.cs (group endpoint shape)
    - Backend/src/TaxReader.Api/Program.cs (lines 352-372 — exact wiring point)
    - Backend/src/TaxReader.Application/Interfaces/IAppDbContext.cs (Database accessor)
    - Backend/src/TaxReader.Infrastructure/Services/AiOnlyClassificationService.cs (line 38 — IAiClassifier.IsConfigured usage)
  </read_first>
  <action>
    Create `Backend/src/TaxReader.Api/Endpoints/HealthEndpoints.cs` (file-scoped namespace `TaxReader.Api.Endpoints`). Public static class `HealthEndpoints` with `public static void MapHealthEndpoints(this WebApplication app)`:

    `/health` (anonymous, top-level): handler injects `IAppDbContext dbContext`, `CancellationToken ct`. `var dbUp = await dbContext.Database.CanConnectAsync(ct);`. Build `var status = dbUp ? "healthy" : "unhealthy";`. Return `Results.Json(new { status, db = dbUp ? "up" : "down" }, statusCode: dbUp ? 200 : 503);`. `.AllowAnonymous().WithName("HealthLiveness").WithTags("Health")`.

    `/api/v1/health` (anonymous, inside the version path): handler injects `IAppDbContext dbContext`, `IAiClassifier aiClassifier`, `CancellationToken ct`. `var dbUp = await dbContext.Database.CanConnectAsync(ct); var anthropicConfigured = aiClassifier.IsConfigured;`. `var healthy = dbUp; // Anthropic-not-configured is reported but does NOT fail liveness — adjust only if research says otherwise`. Return `Results.Json(new { status = healthy ? "healthy" : "unhealthy", db = dbUp ? "up" : "down", anthropic = anthropicConfigured ? "configured" : "unconfigured" }, statusCode: healthy ? 200 : 503);`. `.AllowAnonymous()`. Register this one with the `/api/v1` path prefix — either via `app.MapGet("/api/v1/health", ...)` directly on `app` with `.AllowAnonymous()`, OR add it to the api group and call `.AllowAnonymous()`. Prefer registering BOTH on `app` directly (simplest, both clearly anonymous and outside the RequireAuthorization group).

    SECURITY (RESEARCH Security Domain): the JSON anonymous objects contain ONLY the status/db/anthropic string fields above — never the connection string, JWT secret, Anthropic ApiKey, environment name, or any exception detail. Do NOT add a `try/catch` that echoes `ex.Message`.

    Wire in Program.cs immediately after `app.MapStripeWebhookEndpoint();` (Program.cs:368): add `app.MapHealthEndpoints();` with a comment "OBS-03: anonymous health probes for BetterStack; minimal status body, no secrets".
  </action>
  <verify>
    <automated>cd Backend && dotnet build src/TaxReader.Api/TaxReader.Api.csproj --configuration Release</automated>
  </verify>
  <acceptance_criteria>
    - `HealthEndpoints.cs` contains `CanConnectAsync`, `IAiClassifier`, `.IsConfigured`, and two `.AllowAnonymous()` calls
    - `HealthEndpoints.cs` does NOT contain `ConnectionString`, `ApiKey`, `Secret`, or `ex.Message`
    - `Program.cs` contains `app.MapHealthEndpoints();`
    - `dotnet build` of TaxReader.Api exits 0
  </acceptance_criteria>
  <done>Both health endpoints exist, are anonymous, ping the DB (and report Anthropic config on the v1 one), and return minimal JSON with no secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HealthEndpointTests (WAF) — 200/JSON, anonymity, no-secret-leak</name>
  <behavior>
    - GetHealth_DbReachable_Returns200WithHealthyJson
    - GetHealth_NoAuthHeader_Returns200 (anonymous — not 401)
    - GetApiV1Health_NoAuthHeader_ReturnsHealthyJsonWithAnthropicField
    - HealthBody_DoesNotContainSecretsOrConnectionString (assert response body has none of: "ConnectionString", "Host=", "Password", "sk_live", "whsec", "Jwt", "Secret")
  </behavior>
  <read_first>
    - Backend/tests/TaxReader.UnitTests/Helpers/RateLimitTestFactory.cs (WAF builder — in-memory DB returns CanConnectAsync true)
    - Backend/tests/TaxReader.UnitTests/RateLimiting/RateLimiterTestCollection.cs (collection attribute)
    - Backend/tests/TaxReader.UnitTests/RateLimiting/ (an existing WAF test class for the HttpClient assertion shape)
  </read_first>
  <action>
    Create `Backend/tests/TaxReader.UnitTests/Health/HealthEndpointTests.cs` in `[Collection(RateLimiterTestCollection.Name)]`. Build the factory via `RateLimitTestFactory.BuildFactory()`, `var client = factory.CreateClient();`. (In-memory provider's `CanConnectAsync` returns true → "healthy".)
    - `GetHealth_DbReachable_Returns200WithHealthyJson`: GET "/health" with no Authorization header; assert StatusCode == 200 and body (await `response.Content.ReadAsStringAsync()`) contains "healthy".
    - `GetHealth_NoAuthHeader_Returns200`: explicit anonymity assertion (status NOT 401).
    - `GetApiV1Health_...`: GET "/api/v1/health"; assert 200, body contains "anthropic".
    - `HealthBody_DoesNotContainSecretsOrConnectionString`: read both bodies; assert (case-insensitive) they do NOT contain "connectionstring", "host=", "password", "sk_live", "whsec", "secret".
    Use FluentAssertions. Dispose the client/factory.
  </action>
  <verify>
    <automated>cd Backend && dotnet test tests/TaxReader.UnitTests/TaxReader.UnitTests.csproj --configuration Release --filter "FullyQualifiedName~HealthEndpointTests"</automated>
  </verify>
  <acceptance_criteria>
    - `HealthEndpointTests.cs` carries `[Collection(RateLimiterTestCollection.Name)]`
    - A test asserts the body contains "healthy" and another asserts it does NOT contain "connectionstring"/"secret"/"sk_live"
    - Tests hit both "/health" and "/api/v1/health" WITHOUT an Authorization header and expect 200 (not 401)
    - The filtered `dotnet test` exits 0
  </acceptance_criteria>
  <done>WAF tests prove both endpoints are anonymous, return healthy JSON against a reachable DB, and leak no secrets/connection strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BetterStack probe (public internet) → /health, /api/v1/health | Unauthenticated external probes hit these endpoints; the response body is publicly visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Information Disclosure | /health + /api/v1/health response body | mitigate | Endpoints return ONLY coarse status fields (status/db/anthropic); HealthEndpointTests asserts the body contains no connection string, secret, Anthropic key, or stack trace (RESEARCH Security Domain) |
| T-07-10 | Denial of Service | anonymous health endpoints | accept | DB ping is a cheap CanConnectAsync; endpoints are behind Caddy + existing global rate limiter; low-value target, BetterStack probe cadence is bounded |
| T-07-11 | Spoofing | anonymous access requirement | mitigate | Endpoints MUST be anonymous for unauthenticated probes; tests assert 200-not-401 so they cannot silently regress behind RequireAuthorization (RESEARCH Open Q 2) |
</threat_model>

<verification>
- `dotnet test Backend/tests/TaxReader.UnitTests --filter "FullyQualifiedName~HealthEndpointTests"` is green.
- Manual smoke (optional): `curl -s http://localhost:5190/health` returns `{"status":"healthy",...}` with no secrets.
</verification>

<success_criteria>
- `/health` and `/api/v1/health` exist, are anonymous, ping the DB (v1 also reports Anthropic config), return minimal JSON containing "healthy", and 503 when unhealthy.
- WAF tests prove anonymity + no secret leakage.
- Endpoints are ready for BetterStack keyword monitoring (wired in 07-07).
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-depth-launch-qa/07-03-SUMMARY.md`
</output>
