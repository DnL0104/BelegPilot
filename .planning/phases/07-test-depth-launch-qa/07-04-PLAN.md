---
phase: 07-test-depth-launch-qa
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend/package.json
  - Frontend/vitest.config.mts
  - Frontend/vitest.setup.ts
  - Frontend/src/lib/format.test.ts
  - Frontend/src/lib/api-client.test.ts
  - Frontend/src/components/upload/upload-form.test.tsx
  - Frontend/src/components/receipts/classify-dialog.test.tsx
autonomous: true
requirements: [QA-02]
user_setup: []

must_haves:
  truths:
    - "`cd Frontend && npx vitest run` executes the suite and passes"
    - "format.ts pure functions (formatCurrency de-DE EUR, categoryLabel, statusLabel) are covered"
    - "api-client.ts shared refresh-promise fires /auth/refresh exactly once under N concurrent 401s"
    - "upload-form empty-selection guard and error-restore path are covered with German copy assertions"
    - "classify-dialog confirm/override flow calls the mutation and toasts the German confirmation strings"
  artifacts:
    - path: "Frontend/vitest.config.mts"
      provides: "Vitest 3 config (jsdom, @/* alias via vite-tsconfig-paths, e2e excluded)"
      contains: "jsdom"
    - path: "Frontend/vitest.setup.ts"
      provides: "@testing-library/jest-dom import"
      contains: "@testing-library/jest-dom"
    - path: "Frontend/src/lib/api-client.test.ts"
      provides: "Shared-refresh-promise dedupe test (QA-02 named target)"
      contains: "resetModules"
  key_links:
    - from: "vitest.config.mts"
      to: "@/* alias"
      via: "tsconfigPaths() plugin"
      pattern: "tsconfigPaths"
    - from: "package.json"
      to: "vitest"
      via: "\"test\": \"vitest\" script + devDeps"
      pattern: "\"test\""
---

<objective>
Stand up Vitest 3 + Testing-Library (React 19) for the frontend (which has ZERO tests today) and write the QA-02 unit/component tests: `format.ts` pure functions, the `api-client.ts` shared-refresh-promise dedupe, the `upload-form` state machine, and the `classify-dialog` confirm/override flow. These run on every PR (D-04 — fast, no Docker).

Purpose: First frontend coverage; lock the JWT-refresh dedupe, the upload error-restore path, and the classification-confirm flow against regression.
Output: Vitest config/setup + 4 test files + a `"test"` script, runnable via `npx vitest run`.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-test-depth-launch-qa/07-RESEARCH.md
@.planning/phases/07-test-depth-launch-qa/07-PATTERNS.md

<interfaces>
<!-- CRITICAL: Frontend/AGENTS.md — "This is NOT the Next.js you know." READ the bundled guide
     node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md BEFORE writing config. It is
     authoritative over training data. -->

Install (run in Frontend/, devDeps — RESEARCH Standard Stack, pins verified):
```
npm install -D vitest@3 @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom @testing-library/user-event vite-tsconfig-paths
```
Add to package.json scripts: `"test": "vitest"` (and optionally `"test:run": "vitest run"`).

vitest.config.mts (RESEARCH Pattern 3 — literal):
```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import tsconfigPaths from 'vite-tsconfig-paths'
export default defineConfig({
  plugins: [tsconfigPaths(), react()],   // tsconfigPaths resolves @/* → ./src/*
  test: {
    environment: 'jsdom',
    setupFiles: ['./vitest.setup.ts'],
    globals: true,
    exclude: ['**/node_modules/**', '**/e2e/**'],   // keep Playwright specs out
  },
})
```
vitest.setup.ts: `import '@testing-library/jest-dom'`.

REAL signatures (NOTE: the project has NO RHF+Zod forms — `useForm`/`zodResolver`/`@hookform` have ZERO matches in src despite installed deps. Do NOT write tests against RHF behavior; target the actual implementations below. login/page.tsx and register/page.tsx use plain useState + native required/minLength.):

- Frontend/src/lib/format.ts: `formatCurrency(amount)` → `Intl.NumberFormat("de-DE",{style:"currency",currency:"EUR"})`. `formatDate` → de-DE. `categoryLabel`/`statusLabel` map enum → exact German labels (e.g. "Werbungskosten – Fachliteratur", "Nicht zugeordnet", "Wird verarbeitet"). Pure functions — trivially unit-testable.
- Frontend/src/lib/api-client.ts: module-level `let refreshPromise` (~line 47) dedupes concurrent 401s — only the first 401 calls `tryRefreshToken()`; others await the same promise (~54-68). The refresh POST uses bare `axios` (NOT the `api` instance) ~line 89. To test: reset module state with `vi.resetModules()` between tests; mock axios; fire N concurrent requests that 401; assert `/auth/refresh` called exactly once.
- Frontend/src/components/upload/upload-form.tsx: `useState<File[]>` + `useUploadFiles()` mutation; `handleUpload` clears files optimistically, calls `mutateAsync`, on error restores files + toasts the server `error` field or "Upload fehlgeschlagen. Bitte erneut versuchen." Empty-selection guard toasts "Bitte mindestens eine Datei auswählen".
- Frontend/src/components/receipts/classify-dialog.tsx: `useConfirmClassification()` mutation; `handleConfirm` → `mutateAsync({ itemId, category })` → toast "Klassifizierung bestätigt"; `handleQuickConfirm` (suggestion accept) → "Vorschlag bestätigt". Category Select + 13-category list is the override surface.

Render with @testing-library/react, drive with @testing-library/user-event, MOCK the TanStack Query hooks (useUploadFiles / useConfirmClassification) and `sonner` toast. Toast is `sonner` — mock `toast` from "sonner".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest install + config + format.ts and api-client.ts unit tests</name>
  <read_first>
    - Frontend/AGENTS.md (the "NOT the Next.js you know" warning)
    - Frontend/node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md (authoritative config guide — READ before writing config)
    - Frontend/package.json (scripts + deps to extend)
    - Frontend/tsconfig.json (the @/* path alias tsconfigPaths must resolve)
    - Frontend/src/lib/format.ts (pure functions under test — exact German labels)
    - Frontend/src/lib/api-client.ts (the shared refreshPromise dedupe — module-level state)
  </read_first>
  <action>
    Run the install command from the interfaces block in Frontend/. Add `"test": "vitest"` and `"test:run": "vitest run"` to package.json scripts. Create `Frontend/vitest.config.mts` and `Frontend/vitest.setup.ts` exactly per the interfaces block (read the bundled vitest.md first to confirm no Next.js-16-specific deltas).

    Create `Frontend/src/lib/format.test.ts`:
    - `formatCurrency(1234.5)` returns a de-DE EUR string (assert it contains "1.234,50" and "€" — match the actual Intl output; if non-breaking-space differs, assert via `.toMatch(/1\.234,50/)` and `/€/`).
    - `categoryLabel("WerbungskostenFachliteratur")` === "Werbungskosten – Fachliteratur"; `categoryLabel("Unbekannt")` === "Nicht zugeordnet"; an unknown key returns the input unchanged.
    - `statusLabel("Processing")` === "Wird verarbeitet".

    Create `Frontend/src/lib/api-client.test.ts` (QA-02 named target): use `vi.mock('axios')` and `vi.resetModules()` in `beforeEach` so the module-level `refreshPromise` is fresh per test. Simulate: first protected request returns 401, the refresh POST resolves once with a new token, then the retried request succeeds. Fire N (e.g. 3) concurrent requests that all 401 and assert the mocked `/auth/refresh` POST was called exactly once (the shared-promise dedupe). Confirm the exact axios mock shape against the real `api-client.ts` interceptor implementation before asserting.
  </action>
  <verify>
    <automated>cd Frontend && npx vitest run src/lib/format.test.ts src/lib/api-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Frontend/vitest.config.mts` contains `jsdom`, `tsconfigPaths`, and `exclude` with `**/e2e/**`
    - `Frontend/vitest.setup.ts` contains `@testing-library/jest-dom`
    - `package.json` scripts contain `"test"`
    - `api-client.test.ts` contains `resetModules` and asserts the refresh endpoint is called exactly once
    - `format.test.ts` asserts the de-DE EUR formatting and at least one German category/status label
    - `npx vitest run` on these two files exits 0
  </acceptance_criteria>
  <done>Vitest is installed and configured; format.ts and the JWT shared-refresh-promise are covered and green.</done>
</task>

<task type="auto">
  <name>Task 2: upload-form + classify-dialog component tests</name>
  <read_first>
    - Frontend/node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md (RTL + Next 16 component-test notes)
    - Frontend/src/components/upload/upload-form.tsx (state machine + German copy + useUploadFiles)
    - Frontend/src/components/receipts/classify-dialog.tsx (confirm/quick-confirm + useConfirmClassification + category list)
    - Frontend/src/hooks/use-receipt-files.ts (useUploadFiles signature to mock)
    - Frontend/src/hooks/use-receipt-items.ts (useConfirmClassification signature to mock)
  </read_first>
  <action>
    Create `Frontend/src/components/upload/upload-form.test.tsx`. Mock the `useUploadFiles` hook (return a controllable `mutateAsync`) and mock `toast` from "sonner". Render `<UploadForm />` with @testing-library/react.
    - Empty-selection: trigger upload with no files → assert `toast.error` (or `toast`) called with "Bitte mindestens eine Datei auswählen" and `mutateAsync` NOT called.
    - Error-restore: select a file (drive via the file input / user-event upload), make `mutateAsync` reject → assert files are restored and the German error toast fires ("Upload fehlgeschlagen. Bitte erneut versuchen." or the server `error` field). Match the exact handler logic in upload-form.tsx before asserting.

    Create `Frontend/src/components/receipts/classify-dialog.test.tsx`. Mock `useConfirmClassification` and `toast`. Render the dialog with a sample item.
    - Confirm path: select a category and confirm → assert `mutateAsync` called with `{ itemId, category }` and `toast` shows "Klassifizierung bestätigt".
    - Quick-confirm (suggestion accept) path → assert "Vorschlag bestätigt".
    Drive interactions with @testing-library/user-event. If the dialog requires a TanStack Query/Auth provider wrapper to render, wrap it in a minimal `QueryClientProvider` test harness (create a tiny `renderWithProviders` helper inline).
  </action>
  <verify>
    <automated>cd Frontend && npx vitest run src/components/upload/upload-form.test.tsx src/components/receipts/classify-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `upload-form.test.tsx` asserts "Bitte mindestens eine Datei auswählen" and that `mutateAsync` is not called on empty selection
    - `classify-dialog.test.tsx` asserts the mutation is called with `{ itemId, category }` and the German toast "Klassifizierung bestätigt"
    - Both tests mock their TanStack Query hooks and `sonner` toast (no real network)
    - `npx vitest run` on these two files exits 0
  </acceptance_criteria>
  <done>The upload state-machine guards and the classification confirm/override flow are covered with German-copy assertions and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client → API (axios interceptor) | The refresh-token interceptor handles 401s; a dedupe bug could spam /auth/refresh or leak a stale token across concurrent requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Spoofing/Elevation | api-client.ts refresh dedupe | mitigate | api-client.test.ts asserts a single /auth/refresh call under concurrent 401s — prevents token-refresh races (ASVS V3 session management on the client) |
| T-07-13 | Tampering | upload-form error-restore | accept | UI-only state restoration; server enforces real validation. Tested for UX correctness, not a security control |
| T-07-14 | Information Disclosure | German error copy in tests | accept | Tests assert user-facing strings only; no secrets involved |
</threat_model>

<verification>
- `cd Frontend && npx vitest run` is green across all four files.
- Vitest does NOT pick up `e2e/` specs (excluded in config) — confirmed once 07-05 adds them.
</verification>

<success_criteria>
- Vitest 3 + RTL installed and configured for Next.js 16 with the `@/*` alias resolved.
- format.ts, api-client.ts shared-refresh-promise, upload-form, and classify-dialog covered and green.
- Tests target the REAL implementations (no RHF+Zod assertions — those forms do not exist in the codebase).
- Suite runs fast on every PR (D-04).
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-depth-launch-qa/07-04-SUMMARY.md`
</output>
