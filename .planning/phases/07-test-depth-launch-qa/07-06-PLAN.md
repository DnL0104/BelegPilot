---
phase: 07-test-depth-launch-qa
plan: 06
type: execute
wave: 3
depends_on: [07-01, 07-04, 07-05]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [QA-04, QA-01, QA-02, QA-03]
user_setup: []

must_haves:
  truths:
    - "The DE-localization guard runs in the existing hygiene-check job on every PR and fails the build on bare toLocaleString() (money not via de-DE) in Frontend/src"
    - "Vitest runs on every PR (D-04) — folded into the frontend job or a sibling fast job"
    - "A separate heavy CI job runs the Postgres Testcontainers integration suite (QA-01) + Playwright E2E (QA-03) on push-to-main and on the `run-heavy` PR label (D-03)"
    - "The heavy job installs Playwright browsers (--with-deps) and relies on ubuntu-latest Docker for Testcontainers (no services: postgres block)"
    - "The three existing lightweight jobs still run on every PR unchanged"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "DE guard in hygiene-check, Vitest in fast tier, new gated heavy job for integration + E2E"
      contains: "run-heavy"
  key_links:
    - from: "ci.yml hygiene-check"
      to: "DE-localization guard step"
      via: "bash grep-inside-if on Frontend/src (06-07 template)"
      pattern: "toLocaleString"
    - from: "ci.yml heavy job"
      to: "dotnet test TaxReader.IntegrationTests + npx playwright test"
      via: "on push:main + run-heavy label, Docker + Playwright browsers"
      pattern: "TaxReader.IntegrationTests"
---

<objective>
Wire the Phase 7 test suites into CI per D-03/D-04/D-07: (1) extend the existing `hygiene-check` job with a DE-localization bash guard (QA-04); (2) run Vitest on every PR (fast tier, D-04); (3) add a separate gated "heavy" job that runs the Postgres Testcontainers integration suite (QA-01) and Playwright E2E (QA-03) on push-to-main + a `run-heavy` PR label (D-03). The three existing lightweight jobs stay unchanged and PR-fast.

Purpose: Keep PR feedback fast while spinning up Docker/browsers only when needed. The heavy job being green (QA-01/02/03) is a D-05 hard launch blocker.
Output: An updated `.github/workflows/ci.yml`.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-test-depth-launch-qa/07-RESEARCH.md
@.planning/phases/07-test-depth-launch-qa/07-PATTERNS.md
@.planning/phases/07-test-depth-launch-qa/07-01-PLAN.md
@.planning/phases/07-test-depth-launch-qa/07-04-PLAN.md
@.planning/phases/07-test-depth-launch-qa/07-05-PLAN.md

<interfaces>
<!-- Real ci.yml structure (the file you are editing). -->

Existing jobs (KEEP unchanged, PR-fast): hygiene-check, backend-build-test (dotnet restore/build/test in Backend; setup-dotnet@v4 with cache-dependency-path: Backend/Directory.Packages.props), frontend-lint-build (setup-node@v4 node 22, npm ci, lint, build in Frontend).

Triggers (current): `on: pull_request: branches:[main]` + `push: branches:[main]`. The heavy job must be conditioned with a job-level `if:` — runs only when `github.event_name == 'push'` (to main) OR the PR carries the `run-heavy` label: `if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'run-heavy')`.

06-07 bash-guard template (ci.yml:47-60 — the EXACT skeleton to clone for the DE guard):
```yaml
- name: <name>
  shell: bash
  run: |
    set -e
    if grep -rnE '<pattern>' "<dir>"; then   # grep INSIDE if so no-match exit-1 doesn't abort set -e
      printf '... FAILED ...\n'
      exit 1
    fi
    printf '... passed.\n'
```

DE guard specifics (RESEARCH Code Examples + Pitfall 4): scope to `Frontend/src`, `--include="*.tsx" --include="*.ts"`. Flag bare `toLocaleString(` with no locale arg, e.g. pattern `toLocaleString\(\s*\)`. Money must go through `formatCurrency` / `Intl.NumberFormat("de-DE"…)` (format.ts is the canonical path). Scope TIGHTLY to avoid false positives on code identifiers; do NOT flag generic English variable/import names.

Heavy-job infra (RESEARCH Pitfall 5 + Open Q 1): `runs-on: ubuntu-latest` provides Docker for Testcontainers — do NOT add a `services: postgres` block (Testcontainers manages its own container). Add `npx playwright install --with-deps` + cache `~/.cache/ms-playwright`. Reuse the `actions/setup-dotnet@v4` (cache-dependency-path: Backend/Directory.Packages.props) and `actions/setup-node@v4` (node 22, cache npm) blocks from existing jobs. Commands: `dotnet test Backend/tests/TaxReader.IntegrationTests` and (after the backend + frontend are buildable and a stack is up) `npx playwright test`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DE-localization guard (hygiene-check) + Vitest on every PR</name>
  <read_first>
    - .github/workflows/ci.yml (the file under edit — full structure)
    - .planning/phases/07-test-depth-launch-qa/07-04-PLAN.md (the "test" script Vitest exposes)
    - Frontend/src/lib/format.ts (confirm formatCurrency is the canonical de-DE path the guard protects)
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, add a new step to the `hygiene-check` job AFTER the legal-placeholder step, cloning the 06-07 bash-guard skeleton:
    ```yaml
    - name: Verify German localization — money via de-DE, no bare toLocaleString (QA-04 / D-07)
      shell: bash
      run: |
        set -e
        src="Frontend/src"
        # Money must be formatted via formatCurrency / Intl.NumberFormat("de-DE"…), never bare toLocaleString().
        if grep -rnE 'toLocaleString\(\s*\)' "$src" --include="*.tsx" --include="*.ts"; then
          printf 'Localization guard FAILED: bare toLocaleString() without a locale — use formatCurrency / Intl.NumberFormat("de-DE").\n'
          exit 1
        fi
        printf 'Localization guard passed.\n'
    ```
    (Keep the pattern tight to avoid Pitfall-4 false positives. If a richer English-string heuristic is added, keep it inside the same grep-in-if structure and scope it to JSX text only; the bare-toLocaleString check is the mandatory minimum.)

    Run Vitest on every PR (D-04): add a `Test (Vitest)` step to the existing `frontend-lint-build` job after `Build`:
    ```yaml
    - name: Test (Vitest)
      working-directory: Frontend
      run: npx vitest run
    ```
    (Vitest devDeps install via the existing `npm ci`; `npx vitest run` is non-watch.)
  </action>
  <verify>
    <automated>cd Frontend && npx vitest run && bash -c 'set -e; if grep -rnE "toLocaleString\(\s*\)" Frontend/src --include="*.tsx" --include="*.ts" 2>/dev/null; then echo GUARD_WOULD_FAIL; exit 1; fi; echo guard-ok'</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml` hygiene-check contains a step with `toLocaleString` and the grep-inside-`if` + `exit 1` pattern
    - `ci.yml` frontend-lint-build contains `npx vitest run`
    - The DE guard pattern is scoped with `--include="*.tsx" --include="*.ts"` on `Frontend/src`
    - The local verify command prints `guard-ok` and Vitest is green (no bare toLocaleString in the codebase today; format.ts uses Intl de-DE)
  </acceptance_criteria>
  <done>The DE-localization guard runs on every PR in hygiene-check and Vitest runs in the frontend fast job; both fail the PR on regression.</done>
</task>

<task type="auto">
  <name>Task 2: Gated heavy job — Testcontainers integration (QA-01) + Playwright E2E (QA-03)</name>
  <read_first>
    - .github/workflows/ci.yml (job structure, setup-dotnet/setup-node blocks to reuse)
    - .planning/phases/07-test-depth-launch-qa/07-01-PLAN.md (integration project path + Docker requirement)
    - .planning/phases/07-test-depth-launch-qa/07-05-PLAN.md (playwright config + webServer; backend prerequisite for the E2E run)
    - .planning/phases/07-test-depth-launch-qa/07-RESEARCH.md (Pitfall 5; Open Q 1 — no services: postgres block)
  </read_first>
  <action>
    Add a new job `heavy-suite` to `.github/workflows/ci.yml`:
    - `name: Heavy suite (Postgres integration + E2E)`, `runs-on: ubuntu-latest`.
    - Job-level gate: `if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'run-heavy')`.
    - Steps:
      1. `actions/checkout@v4`.
      2. `actions/setup-dotnet@v4` (dotnet-version '10.0.x', cache: true, cache-dependency-path: Backend/Directory.Packages.props).
      3. Run the integration suite: `working-directory: Backend`, `run: dotnet test tests/TaxReader.IntegrationTests/TaxReader.IntegrationTests.csproj --configuration Release` (ubuntu-latest Docker powers Testcontainers; NO `services: postgres` block — Testcontainers manages its own postgres:17-alpine container).
      4. `actions/setup-node@v4` (node 22, cache npm, cache-dependency-path: Frontend/package-lock.json).
      5. `npm ci` in Frontend.
      6. Cache Playwright browsers: `actions/cache@v4` keyed on `~/.cache/ms-playwright` + the Playwright version (or package-lock hash).
      7. `npx playwright install --with-deps` in Frontend.
      8. Run E2E: bring up the stack the Playwright `webServer`/proxy needs. The Next `webServer` builds+starts the frontend; the backend must also be reachable for the real happy path. Start the backend (e.g. `docker compose up -d db api` or run the API with a test Postgres) BEFORE `npx playwright test`. If a full compose-up is impractical in this step, document the chosen approach inline and ensure the E2E run targets the real backend (do NOT mock). Command: `working-directory: Frontend`, `run: npx playwright test`.
      9. Upload the Playwright report on failure: `actions/upload-artifact@v4` with `if: failure()`, `name: playwright-report`, `path: Frontend/playwright-report/`.
    Keep the three lightweight jobs untouched. Confirm the YAML parses (no tabs; 2-space indentation matching the existing file).
  </action>
  <verify>
    <automated>npx --yes js-yaml .github/workflows/ci.yml > /dev/null 2>&1 && echo ci.yml-valid-yaml || (echo INVALID-YAML && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml` contains a `heavy-suite` job with `if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'run-heavy')`
    - The heavy job contains `dotnet test` against `TaxReader.IntegrationTests` and `npx playwright install --with-deps` and `npx playwright test`
    - The heavy job has NO `services:` postgres block (Testcontainers manages its own)
    - The three original jobs (hygiene-check, backend-build-test, frontend-lint-build) are unchanged in trigger
    - `ci.yml` is valid YAML
  </acceptance_criteria>
  <done>A gated heavy job runs the Postgres integration suite and Playwright E2E on push-to-main and the run-heavy label, installing Docker-backed containers + Playwright browsers; PR feedback stays fast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → secrets/test infra | The heavy job provisions ephemeral Postgres + browsers; no production secrets should be required to run the test suites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Tampering | CI gate bypass | mitigate | The heavy suite (QA-01/02/03) is a D-05 hard launch blocker; gating it on push-to-main ensures it runs before any main deploy, and the run-heavy label allows pre-merge verification |
| T-07-19 | Information Disclosure | DE guard false-negative | accept | The guard is a regression net, not a security control; the one-time native review (07-07, non-blocking per D-06) backstops linguistic gaps |
| T-07-20 | Denial of Service | heavy job cost | accept | Gated to push-to-main + opt-in label so Docker/browser spin-up does not run on every PR (D-03) — bounds runner minutes for a solo dev |
</threat_model>

<verification>
- `ci.yml` is valid YAML; the heavy job gate evaluates correctly (push to main OR run-heavy label).
- Locally: Vitest green + DE guard prints guard-ok; the integration + E2E commands are the same ones 07-01/07-05 verify.
</verification>

<success_criteria>
- DE-localization guard runs in hygiene-check on every PR (QA-04); fails on bare toLocaleString.
- Vitest runs on every PR (D-04); the heavy job (QA-01 integration + QA-03 E2E) runs gated on push-to-main + run-heavy label (D-03).
- The three lightweight jobs remain PR-fast and unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-depth-launch-qa/07-06-SUMMARY.md`
</output>
