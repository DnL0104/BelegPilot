---
phase: 07-test-depth-launch-qa
plan: 07
type: execute
wave: 4
depends_on: [07-03, 07-06]
files_modified:
  - PITFALLS.md
  - .planning/phases/07-test-depth-launch-qa/07-GO-NO-GO.md
  - .planning/phases/07-test-depth-launch-qa/07-OPS-SETUP.md
  - .planning/phases/07-test-depth-launch-qa/07-HUMAN-UAT.md
autonomous: false
requirements: [QA-06, QA-07, OBS-03, QA-05]
user_setup:
  - service: betterstack
    why: "Uptime monitors on /health + /api/v1/health, status page linked from footer, deploy maintenance windows (OBS-03 / D-08)"
    env_vars:
      - name: BETTERSTACK_API_TOKEN
        source: "BetterStack Dashboard -> API tokens (only if provisioning monitors via API; dashboard setup needs no env var)"
    dashboard_config:
      - task: "Create keyword monitors on https://<domain>/health and /api/v1/health asserting the body contains 'healthy'; set quiet-hours / maintenance windows so deploys don't page"
        location: "BetterStack -> Uptime -> Monitors"
      - task: "Create a status page and link it from the site footer"
        location: "BetterStack -> Status pages"
  - service: sentry
    why: "Tune alert rules vs real-traffic baseline; quiet hours 23:00-07:00 = HIGH-severity pages only (QA-06 / D-08)"
    dashboard_config:
      - task: "Set quiet-hours alert rule: 23:00-07:00 Europe/Berlin only HIGH severity pages; default channel email + push"
        location: "Sentry -> Alerts -> Alert rules"

must_haves:
  truths:
    - "PITFALLS.md exists as the canonical 'Looks done but isn't' pre-launch checklist, seeded from the 7 RESEARCH pitfalls + the D-05 hard-blocker list"
    - "07-GO-NO-GO.md enumerates every D-05 hard blocker (automated suites green; lawyer sign-off; Phase 6 operator items closed incl. all four AVVs) with a current status, and the D-06 non-blocking items tracked separately"
    - "07-OPS-SETUP.md gives the operator exact steps to wire BetterStack monitors (keyword on 'healthy') + status page + maintenance windows, and the Sentry quiet-hours rule"
    - "07-HUMAN-UAT.md captures the manual-only items: native-speaker DE review, mobile phone-camera upload, lawyer sign-off, AVV signing"
  artifacts:
    - path: "PITFALLS.md"
      provides: "Pre-launch 'looks done but isn't' checklist (QA-07)"
      contains: "Looks done but isn"
    - path: ".planning/phases/07-test-depth-launch-qa/07-GO-NO-GO.md"
      provides: "Launch go/no-go decision record with D-05 blockers + D-06 non-blocking items"
      contains: "D-05"
    - path: ".planning/phases/07-test-depth-launch-qa/07-OPS-SETUP.md"
      provides: "BetterStack + Sentry operator wiring instructions (OBS-03 / QA-06)"
      contains: "keyword"
  key_links:
    - from: "07-OPS-SETUP.md"
      to: "/health + /api/v1/health (from 07-03)"
      via: "BetterStack keyword monitor on 'healthy'"
      pattern: "api/v1/health"
    - from: "07-GO-NO-GO.md"
      to: "heavy CI suite (07-06) + lawyer review + AVVs"
      via: "D-05 hard-blocker checklist"
      pattern: "D-05"
---

<objective>
Close the phase with the launch readiness artifacts: author `PITFALLS.md` (the QA-07 "Looks done but isn't" checklist), the `07-GO-NO-GO.md` launch decision record (D-05 hard blockers vs D-06 non-blocking), the `07-OPS-SETUP.md` operator instructions for BetterStack monitors + status page + maintenance windows and Sentry quiet-hours (QA-06 / OBS-03 / D-08), and the `07-HUMAN-UAT.md` capturing the inherently-manual items. Pause at a checkpoint for the operator to wire the external dashboards and confirm the go/no-go status.

Purpose: Several launch gates are external/manual (BetterStack dashboard, Sentry rules, lawyer, AVVs); these cannot be code-automated. This plan makes them explicit, trackable, and operator-actionable so the milestone closes on a real go/no-go decision.
Output: PITFALLS.md + three planning docs + a human checkpoint.
</objective>

<execution_context>
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/workflows/execute-plan.md
@D:/Programming/Repos/TaxReader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-test-depth-launch-qa/07-CONTEXT.md
@.planning/phases/07-test-depth-launch-qa/07-RESEARCH.md
@.planning/phases/07-test-depth-launch-qa/07-VALIDATION.md
@.planning/phases/06-legal-consent-data-export/06-HUMAN-UAT.md
@.planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md
@.planning/phases/06-legal-consent-data-export/06-AVV-TRACKING.md

<interfaces>
<!-- Source material for the docs. -->

D-05 HARD launch blockers (must be GREEN before "go"):
1. All automated suites green in CI: QA-01 Postgres integration + QA-02 Vitest + QA-03 Playwright E2E (the 07-06 heavy job).
2. Final lawyer sign-off on AGB + Datenschutzerklaerung (QA-07); draft markers removed.
3. Phase 6 operator items closed: real Impressum/legal contact data filled (the 06-07 CI placeholder guard goes green) AND all four AVVs/DPAs signed (Anthropic, Stripe, Sentry, BetterStack).

D-06 NON-blocking (track in go/no-go, do NOT gate): native-speaker DE polish review beyond the automated guard; prior-phase (Phases 2/3/4) HUMAN-UAT debt.

D-08 monitoring: BetterStack monitors on /health (DB ping) + /api/v1/health (DB + Anthropic config) — built in 07-03, returning JSON containing "healthy"; status page linked from footer; deploy-maintenance windows configurable. Sentry quiet hours 23:00-07:00 = HIGH-severity pages only; channel email + push (RESEARCH A4 default).

RESEARCH "Common Pitfalls" (7 traps to seed PITFALLS.md): (1) Hangfire schema init in test host; (2) parallel WAF crash; (3) Respawn deletes EF/Hangfire tables; (4) DE guard false positives; (5) Playwright/Testcontainers infra missing in CI; (6) BetterStack status-code-only monitor misses degradation; (7) in-memory idempotency false confidence. Plus launch-specific "looks done but isn't" items: health endpoints leak no secrets; both health endpoints anonymous; refresh-token replay revokes-all against real DB; payment idempotency real UNIQUE; export download one-time-token IDOR (LEG-07); cookie banner equally-prominent options; legal placeholders filled.

Health endpoints (from 07-03): /health and /api/v1/health, anonymous, JSON body with "healthy". BetterStack must use a KEYWORD monitor on "healthy" (not status-code-only — RESEARCH Pitfall 6) OR rely on the 503-when-unhealthy behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PITFALLS.md + 07-GO-NO-GO.md + 07-OPS-SETUP.md + 07-HUMAN-UAT.md</name>
  <read_first>
    - .planning/phases/07-test-depth-launch-qa/07-RESEARCH.md (the 7 "Common Pitfalls" + Security Domain to seed PITFALLS.md)
    - .planning/phases/07-test-depth-launch-qa/07-CONTEXT.md (D-05 / D-06 / D-08 verbatim)
    - .planning/phases/07-test-depth-launch-qa/07-VALIDATION.md (Manual-Only Verifications table to mirror into 07-HUMAN-UAT.md)
    - .planning/phases/06-legal-consent-data-export/06-HUMAN-UAT.md (Phase 6 operator/lawyer/UI items that become D-05 blockers / D-06 debt)
    - .planning/phases/06-legal-consent-data-export/06-AVV-TRACKING.md (the four AVVs — Anthropic, Stripe, Sentry, BetterStack)
    - .planning/phases/06-legal-consent-data-export/06-LEGAL-REVIEW.md (the lawyer-review gate doc)
    - Frontend/src (confirm the footer component for the BetterStack status-page link instruction)
  </read_first>
  <action>
    Create `PITFALLS.md` (repo root) titled with the phrase "Looks done but isn't" — a pre-launch verification checklist. Sections, each item a `[ ]` checkbox with its verification method: (a) Test-infra traps (the 7 RESEARCH pitfalls, e.g. "[ ] Payment idempotency asserted against REAL Postgres UNIQUE, not in-memory (Pitfall 7)"); (b) Security must-not-leak (health endpoints contain no connection string/secret/Anthropic key; both health endpoints anonymous; refresh-token replay revokes-all against real DB; export one-time-token IDOR holds); (c) Localization (no bare toLocaleString; money via de-DE; native review done — non-blocking); (d) Legal/launch (legal placeholders filled so 06-07 guard is green; lawyer sign-off; four AVVs signed); (e) Ops (BetterStack keyword monitors live; Sentry quiet-hours set).

    Create `.planning/phases/07-test-depth-launch-qa/07-GO-NO-GO.md`: two tables — "Hard blockers (D-05)" with columns Item | Source | Status | Evidence, listing the three D-05 blockers expanded (CI heavy green; lawyer sign-off; legal data filled + four AVVs); and "Non-blocking (D-06)" listing native-speaker review + Phase 2/3/4 UAT debt. A final "Decision: GO / NO-GO" line left for the operator, with the rule: GO only when ALL D-05 rows are green.

    Create `.planning/phases/07-test-depth-launch-qa/07-OPS-SETUP.md`: exact operator steps for (1) BetterStack — create KEYWORD monitors on `https://<domain>/health` and `https://<domain>/api/v1/health` asserting body contains "healthy" (RESEARCH Pitfall 6), configure deploy maintenance windows, create a status page and link it from the site footer (name the footer component to edit); (2) Sentry — quiet-hours alert rule 23:00-07:00 Europe/Berlin, HIGH-severity pages only, channel email + push. Note these are external-dashboard actions Claude cannot automate (no public provisioning API assumed; if BETTERSTACK_API_TOKEN is provided later an API path can be scripted — out of scope now).

    Create `.planning/phases/07-test-depth-launch-qa/07-HUMAN-UAT.md`: the manual-only items table (mirroring 07-VALIDATION.md "Manual-Only Verifications") — native-speaker DE polish review (QA-04, non-blocking per D-06), mobile phone-camera photo-receipt upload end-to-end at sm/md (QA-05, the non-automatable part — viewport smoke is automated in 07-05), lawyer sign-off on AGB + Datenschutzerklaerung (QA-07, HARD blocker), AVV/DPA signing for all four sub-processors (QA-07, HARD blocker). Each row: Behavior | Requirement | Blocking? | Instructions.
  </action>
  <verify>
    <automated>bash Backend/../scripts/no-such || test -s PITFALLS.md && test -s .planning/phases/07-test-depth-launch-qa/07-GO-NO-GO.md && test -s .planning/phases/07-test-depth-launch-qa/07-OPS-SETUP.md && test -s .planning/phases/07-test-depth-launch-qa/07-HUMAN-UAT.md && grep -qi "looks done but" PITFALLS.md && grep -q "D-05" .planning/phases/07-test-depth-launch-qa/07-GO-NO-GO.md && grep -qi "keyword" .planning/phases/07-test-depth-launch-qa/07-OPS-SETUP.md && echo docs-ok</automated>
  </verify>
  <acceptance_criteria>
    - `PITFALLS.md` exists, contains the phrase "Looks done but isn" (the checklist title) and all 7 RESEARCH pitfalls as checkable items
    - `07-GO-NO-GO.md` contains a "Hard blockers (D-05)" table with the three blockers and a GO/NO-GO decision line
    - `07-OPS-SETUP.md` contains "keyword" and both `/health` and `/api/v1/health` and a Sentry quiet-hours instruction
    - `07-HUMAN-UAT.md` lists native-speaker review, mobile phone-camera upload, lawyer sign-off, and AVV signing with a Blocking? column
    - The verify command prints `docs-ok`
  </acceptance_criteria>
  <done>All four launch-readiness docs exist and are populated from the source artifacts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator wires BetterStack + Sentry; sets the go/no-go decision</name>
  <what-built>07-OPS-SETUP.md (BetterStack + Sentry instructions), 07-GO-NO-GO.md (D-05/D-06 status), 07-HUMAN-UAT.md, and PITFALLS.md. The health endpoints (/health, /api/v1/health) are live from 07-03; the CI heavy suite is wired from 07-06.</what-built>
  <how-to-verify>
    1. Follow 07-OPS-SETUP.md to create BetterStack KEYWORD monitors on https://<your-domain>/health and /api/v1/health asserting the body contains "healthy"; confirm each monitor reports "up". Create a status page and link it from the site footer. Configure a deploy maintenance window.
    2. In Sentry, set the quiet-hours alert rule (23:00-07:00 Europe/Berlin, HIGH-severity only, email + push).
    3. Confirm the D-05 hard blockers in 07-GO-NO-GO.md: (a) the CI heavy suite (QA-01 + QA-02 + QA-03) is green on main; (b) lawyer sign-off on AGB + Datenschutzerklaerung is recorded and draft markers removed; (c) the 06-07 legal-placeholder CI guard is green (real contact data filled) AND all four AVVs (Anthropic, Stripe, Sentry, BetterStack) are signed.
    4. Run the mobile phone-camera photo-receipt upload UAT at sm/md (QA-05) and note the result (non-blocking).
    5. Set the "Decision: GO / NO-GO" line in 07-GO-NO-GO.md. GO only if ALL D-05 rows are green.
  </how-to-verify>
  <resume-signal>Reply "go" (all D-05 green), "no-go: <reasons>", or "approved — tracked" to record the current status and continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BetterStack probe (public internet) -> health endpoints | Public unauthenticated monitoring; the keyword monitor must assert real health, not just a 200 |
| Operator -> external dashboards (BetterStack/Sentry) | Manual config holds the monitoring/alerting posture; mis-config silently degrades ops readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Denial of Service / availability blind spot | BetterStack monitor config | mitigate | 07-OPS-SETUP.md mandates a KEYWORD monitor on "healthy" (or 503-on-unhealthy) so a DB outage behind a 200 is caught (RESEARCH Pitfall 6) |
| T-07-22 | Repudiation / launch with unmet gate | go/no-go decision | mitigate | 07-GO-NO-GO.md makes D-05 blockers explicit and requires ALL green before GO; the checkpoint forces an operator decision rather than an implicit launch |
| T-07-23 | Information Disclosure | status-page exposure | accept | Status page shows only up/down + maintenance windows; no secrets. BetterStack-hosted; bounded for a solo product |
</threat_model>

<verification>
- All four docs exist and are populated (Task 1 verify command prints docs-ok).
- The checkpoint records the operator's BetterStack/Sentry wiring + the go/no-go decision.
- PITFALLS.md is end-to-end walkable as the QA-07 pre-launch checklist.
</verification>

<success_criteria>
- PITFALLS.md authored (QA-07); 07-GO-NO-GO.md tracks D-05 hard blockers + D-06 non-blocking; 07-OPS-SETUP.md gives exact BetterStack + Sentry wiring (QA-06 / OBS-03); 07-HUMAN-UAT.md captures manual items incl. QA-05 phone-camera + lawyer/AVV blockers.
- Operator has wired BetterStack monitors (keyword on "healthy") + status page + maintenance windows and the Sentry quiet-hours rule, and set a go/no-go decision.
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-depth-launch-qa/07-07-SUMMARY.md`
</output>
